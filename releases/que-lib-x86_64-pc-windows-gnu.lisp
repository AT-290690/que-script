(do (let *Id* (vector 0)) (let Id! (lambda (do (let id (&get *Id*)) (&alter! *Id* (+ (&get *Id*) 1)) id))) (let const/int/max-safe 2147483647) (let const/int/min-safe -2147483648) (let const/dec/max-safe 2147483.8) (let const/dec/min-safe -2147483.8) (let const/dec/pi 3.142) (let const/dec/ln2 0.693) (let infinity 2147483647) (let -infinity -2147483648) (let Int 0) (let Dec 0.0) (let Char (&get (string 97))) (let Bool false) (let Nil nil) (let as (lambda _ t t)) (let : (lambda t x (do (vector t x) x))) (let eq (lambda a b (cond (and a b) true (and (not a) (not b)) true false))) (let identity (lambda x x)) (let Char/nil (Int->Char 0)) (let Char/start (Int->Char 2)) (let Char/end (Int->Char 3)) (let const/dec/two-pi (+. const/dec/pi const/dec/pi)) (let const/dec/wrap-pi (lambda x (do (mut y x) (while (>. y const/dec/pi) (do (alter! y (-. y const/dec/two-pi)))) (while (<. y (-. const/dec/pi)) (do (alter! y (+. y const/dec/two-pi)))) y))) (let const/dec/sin/terms (lambda x terms (do (let y (const/dec/wrap-pi x)) (let x2 (*. y y)) (mut term y) (mut sm y) (mut n 0) (while (< n terms) (do (let a (+ (* 2 n) 2)) (let b (+ (* 2 n) 3)) (let denom (Int->Dec (* a b))) (alter! term (/. (-. (*. term x2)) denom)) (alter! sm (+. sm term)) (alter! n (+ n 1)))) sm))) (let const/dec/cos/terms (lambda x terms (do (let y (const/dec/wrap-pi x)) (let x2 (*. y y)) (mut term 1.0) (mut sm 1.0) (mut n 0) (while (< n terms) (do (let a (+ (* 2 n) 1)) (let b (+ (* 2 n) 2)) (let denom (Int->Dec (* a b))) (alter! term (/. (-. (*. term x2)) denom)) (alter! sm (+. sm term)) (alter! n (+ n 1)))) sm))) (let sin (lambda x (const/dec/sin/terms x 8))) (let cos (lambda x (const/dec/cos/terms x 8))) (let &box (lambda value (vector value))) (let &alter! (lambda vrbl x (set! vrbl 0 x))) (let &get (lambda vrbl (get vrbl 0))) (let const/int/byte-off (lambda x shift (& (>> x shift) 255))) (let const/int/pack-u32 (lambda b0 b1 b2 b3 (| b0 (| (<< b1 8) (| (<< b2 16) (<< b3 24)))))) (let const/int/u32/add (lambda a b (do (let s0 (+ (const/int/byte-off a 0) (const/int/byte-off b 0))) (let r0 (& s0 255)) (let c0 (>> s0 8)) (let s1 (+ (const/int/byte-off a 8) (const/int/byte-off b 8) c0)) (let r1 (& s1 255)) (let c1 (>> s1 8)) (let s2 (+ (const/int/byte-off a 16) (const/int/byte-off b 16) c1)) (let r2 (& s2 255)) (let c2 (>> s2 8)) (let s3 (+ (const/int/byte-off a 24) (const/int/byte-off b 24) c2)) (let r3 (& s3 255)) (const/int/pack-u32 r0 r1 r2 r3)))) (let const/int/u32/urshift (lambda x n (if (= n 0) x (& (>> x n) (- (<< 1 (- 32 n)) 1))))) (let const/int/u32/mul (lambda a b (do (let a0 (const/int/byte-off a 0)) (let a1 (const/int/byte-off a 8)) (let a2 (const/int/byte-off a 16)) (let a3 (const/int/byte-off a 24)) (let b0 (const/int/byte-off b 0)) (let b1 (const/int/byte-off b 8)) (let b2 (const/int/byte-off b 16)) (let b3 (const/int/byte-off b 24)) (let s0 (* a0 b0)) (let r0 (& s0 255)) (let c0 (>> s0 8)) (let s1 (+ (* a0 b1) (* a1 b0) c0)) (let r1 (& s1 255)) (let c1 (>> s1 8)) (let s2 (+ (* a0 b2) (* a1 b1) (* a2 b0) c1)) (let r2 (& s2 255)) (let c2 (>> s2 8)) (let s3 (+ (* a0 b3) (* a1 b2) (* a2 b1) (* a3 b0) c2)) (let r3 (& s3 255)) (const/int/pack-u32 r0 r1 r2 r3)))) (let const/dec/u32 (lambda x (do (mut acc (Int->Dec (const/int/byte-off x 0))) (alter! acc (+. (/. acc 256.0) (Int->Dec (const/int/byte-off x 8)))) (alter! acc (+. (/. acc 256.0) (Int->Dec (const/int/byte-off x 16)))) (alter! acc (+. (/. acc 256.0) (Int->Dec (const/int/byte-off x 24)))) (/. acc 256.0)))) (let const/int/mulberry32/raw (lambda seed (do (let next-seed (const/int/u32/add seed 1831565813)) (let z1 (const/int/u32/mul (^ next-seed (const/int/u32/urshift next-seed 15)) (| next-seed 1))) (let z2 (^ z1 (const/int/u32/add z1 (const/int/u32/mul (^ z1 (const/int/u32/urshift z1 7)) (| z1 61))))) (tuple next-seed (^ z2 (const/int/u32/urshift z2 14)))))) (let const/dec/mulberry32/next (lambda seed (do (let step (const/int/mulberry32/raw seed)) (tuple (fst step) (const/dec/u32 (snd step)))))) (let random/int const/int/mulberry32/raw) (let random/dec const/dec/mulberry32/next) (let std/dec/log (lambda x (if (<=. x 0.0) 0.0 (do (mut y x) (mut shifts 0) (while (>. y 2.0) (do (alter! y (/. y 2.0)) (alter! shifts (+ shifts 1)))) (while (<. y 1.0) (do (alter! y (*. y 2.0)) (alter! shifts (- shifts 1)))) (let z (/. (-. y 1.0) (+. y 1.0))) (let z2 (*. z z)) (mut term z) (mut sm z) (mut n 1) (while (< n 12) (do (alter! term (*. term z2)) (alter! sm (+. sm (/. term (Int->Dec (+ (* 2 n) 1))))) (alter! n (+ n 1)))) (+. (*. 2.0 sm) (*. (Int->Dec shifts) const/dec/ln2)))))) (let Payload/get (lambda (i) (if (> (length ARGV) i) (get ARGV i) (string)))) (let Meta/token->Pair (lambda (token) (let key (vector)) (let value (vector)) (mut seen? false) (mut i 0) (while (< i (length token)) (let ch (get token i)) (if (and (not seen?) (=# ch (char 61))) (alter! seen? true) (if seen? (push! value ch) (push! key ch))) (++ i)) (tuple key value))) (let Meta/parse (lambda (text) (let out (vector)) (for (lambda (token) (if (not-empty? token) (push! out (Meta/token->Pair token)))) (split (string 32) text)) out)) (let Meta/table (lambda (text) (reduce (lambda (acc pair) (Table/set! acc (fst pair) (snd pair)) acc) (Table/new) (Meta/parse text)))) (let Meta/has? (lambda (table key) (Table/has? key table))) (let Meta/get (lambda (table key) (if (Table/has? key table) (Table/get-unsafe key table) (string)))) (let Meta/get/text (lambda (text key) (Meta/get (Meta/table text) key))) (let std/int/prime? (lambda n (cond (< n 2) false (= n 2) true (= n 3) true (= (mod n 2) 0) false (= (mod n 3) 0) false (do (mut i 5) (mut ok true) (while (and ok (<= (* i i) n)) (do (if (or (= (mod n i) 0) (= (mod n (+ i 2)) 0)) (alter! ok false)) (alter! i (+ i 6)))) ok)))) (letmacro unless ((con) (qq (if (uq con) nil nil))) ((con body) (qq (if (uq con) nil (uq body)))) ((con then else) (qq (if (uq con) (uq else) (uq then))))) (letmacro != (lambda a b (qq (not (= (uq a) (uq b)))))) (letmacro <> (lambda a b (qq (not (= (uq a) (uq b)))))) (letmacro cond (() (qq 0)) ((default) (qq (uq default))) ((test branch) (qq (if (uq test) (uq branch) nil))) ((test branch . rest) (qq (if (uq test) (uq branch) (cond (uqs rest)))))) (letmacro when (lambda con . body (qq (if (uq con) (do (uqs body)) nil)))) (letmacro when-not (lambda con . body (qq (unless (uq con) (do (uqs body)))))) (letmacro += (lambda x n (qq (alter! (uq x) (+ (uq x) (uq n)))))) (letmacro -= (lambda x n (qq (alter! (uq x) (- (uq x) (uq n)))))) (letmacro *= (lambda x n (qq (alter! (uq x) (* (uq x) (uq n)))))) (letmacro /= (lambda x n (qq (alter! (uq x) (/ (uq x) (uq n)))))) (letmacro ++ (lambda x (qq (alter! (uq x) (+ (uq x) 1))))) (letmacro -- (lambda x (qq (alter! (uq x) (- (uq x) 1))))) (letmacro loop/for (lambda name init con step . body (qq (do (mut (uq name) (uq init)) (while (uq con) (do (uqs body) (uq step) nil)))))) (letmacro loop/range/exclusive (lambda name start end . body (qq (do (mut (uq name) (uq start)) (while (< (uq name) (uq end)) (do (uqs body) (++ (uq name)) nil)))))) (letmacro loop/range/inclusive (lambda name start end . body (qq (do (mut (uq name) (uq start)) (while (<= (uq name) (uq end)) (do (uqs body) (++ (uq name)) nil)))))) (letmacro loop/range (lambda name start end . body (qq (loop/range/inclusive (uq name) (uq start) (uq end) (uqs body))))) (letmacro loop/range/inclusive/by (lambda name start end step . body (qq (do (mut (uq name) (uq start)) (let step# (uq step)) (if (> step# 0) (while (<= (uq name) (uq end)) (do (uqs body) (+= (uq name) step#) nil)) (while (>= (uq name) (uq end)) (do (uqs body) (+= (uq name) step#) nil))))))) (letmacro loop/range/exclusive/by (lambda name start end step . body (qq (do (mut (uq name) (uq start)) (let step# (uq step)) (if (> step# 0) (while (< (uq name) (uq end)) (do (uqs body) (+= (uq name) step#) nil)) (while (> (uq name) (uq end)) (do (uqs body) (+= (uq name) step#) nil))))))) (letmacro loop/range/by (lambda name start end step . body (qq (loop/range/inclusive/by (uq name) (uq start) (uq end) (uq step) (uqs body))))) (letmacro times (lambda n . body (do (let i (gensym)) (qq (loop/range/exclusive (uq i) 0 (uq n) (uqs body)))))) (letmacro repeat (lambda n body (do (let i (gensym)) (qq (loop/range/exclusive (uq i) 0 (uq n) (uq body)))))) (letmacro loop (lambda start end fn (do (let i (gensym)) (let loop-end (gensym)) (let loop-cb (gensym)) (qq (do (mut (uq i) (uq start)) (let (uq loop-end) (uq end)) (let (uq loop-cb) (uq fn)) (while (< (uq i) (uq loop-end)) (do ((uq loop-cb) (uq i)) (alter! (uq i) (+ (uq i) 1)) nil))))))) (letmacro let* ((name value body) (qq (block (let (uq name) (uq value)) (uq body)))) ((name value . rest) (qq (block (let (uq name) (uq value)) (let* (uqs rest)))))) (letmacro block (lambda . body (qq ((lambda (do (uqs body))))))) (let __type/vec1 (lambda a x (do (vector a x) x))) (let __type/fn1 (lambda in out f (do (let probe (lambda x (__type/vec1 out (f (__type/vec1 in x))))) f))) (let __type/fn2 (lambda a b out f (do (let probe (lambda x y (__type/vec1 out (f (__type/vec1 a x) (__type/vec1 b y))))) f))) (let __type/fn3 (lambda a b c out f (do (let probe (lambda x y z (__type/vec1 out (f (__type/vec1 a x) (__type/vec1 b y) (__type/vec1 c z))))) f))) (let __type/fn4 (lambda a b c d out f (do (let probe (lambda x y z w (__type/vec1 out (f (__type/vec1 a x) (__type/vec1 b y) (__type/vec1 c z) (__type/vec1 d w))))) f))) (let __type/fn5 (lambda a b c d e out f (do (let probe (lambda x y z w v (__type/vec1 out (f (__type/vec1 a x) (__type/vec1 b y) (__type/vec1 c z) (__type/vec1 d w) (__type/vec1 e v))))) f))) (let __type/fn6 (lambda a b c d e g out f (do (let probe (lambda x y z w v u (__type/vec1 out (f (__type/vec1 a x) (__type/vec1 b y) (__type/vec1 c z) (__type/vec1 d w) (__type/vec1 e v) (__type/vec1 g u))))) f))) (letmacro Fn1 (lambda A R (qq (lambda f (__type/fn1 (uq A) (uq R) f))))) (letmacro Fn2 (lambda A B R (qq (lambda f (__type/fn2 (uq A) (uq B) (uq R) f))))) (letmacro Fn3 (lambda A B C R (qq (lambda f (__type/fn3 (uq A) (uq B) (uq C) (uq R) f))))) (letmacro Fn4 (lambda A B C D R (qq (lambda f (__type/fn4 (uq A) (uq B) (uq C) (uq D) (uq R) f))))) (letmacro Fn5 (lambda A B C D E R (qq (lambda f (__type/fn5 (uq A) (uq B) (uq C) (uq D) (uq E) (uq R) f))))) (letmacro Fn6 (lambda A B C D E G R (qq (lambda f (__type/fn6 (uq A) (uq B) (uq C) (uq D) (uq E) (uq G) (uq R) f))))) (letmacro letype (lambda Name Ty (qq (let (uq Name) (lambda x (__type/vec1 (uq Ty) x)))))) (letmacro sig (lambda Ty value (qq ((uq Ty) (uq value))))) (letmacro letype/fn1 (lambda Name A R (qq (let (uq Name) (Fn1 (uq A) (uq R)))))) (letmacro letype/fn2 (lambda Name A B R (qq (let (uq Name) (Fn2 (uq A) (uq B) (uq R)))))) (letmacro letype/fn3 (lambda Name A B C R (qq (let (uq Name) (Fn3 (uq A) (uq B) (uq C) (uq R)))))) (letmacro letype/fn4 (lambda Name A B C D R (qq (let (uq Name) (Fn4 (uq A) (uq B) (uq C) (uq D) (uq R)))))) (letmacro letype/fn5 (lambda Name A B C D E R (qq (let (uq Name) (Fn5 (uq A) (uq B) (uq C) (uq D) (uq E) (uq R)))))) (letmacro letype/fn6 (lambda Name A B C D E G R (qq (let (uq Name) (Fn6 (uq A) (uq B) (uq C) (uq D) (uq E) (uq G) (uq R)))))) (let std/char/empty (get (string 0) 0)) (let std/char/double-quote (get (string 34) 0)) (let std/char/single-quote (get (string 39) 0)) (let std/char/new-line (get (string 10) 0)) (let std/char/space (get (string 32) 0)) (let std/char/tab (get (string 32 32) 0)) (let std/char/comma (get (string 44) 0)) (let std/char/dot (get (string 46) 0)) (let std/char/semi-colon (get (string 59) 0)) (let std/char/colon (get (string 58) 0)) (let std/char/dash (get (string 45) 0)) (let std/char/lower-dash (get (string 95) 0)) (let std/char/left-brace (get (string 40) 0)) (let std/char/right-brace (get (string 41) 0)) (let std/char/curly-left-brace (get (string 123) 0)) (let std/char/curly-right-brace (get (string 125) 0)) (let std/char/left-bracket (get (string 91) 0)) (let std/char/right-bracket (get (string 93) 0)) (let std/char/pipe (get (string 124) 0)) (let std/char/hash (get (string 35) 0)) (let std/char/question-mark (get (string 63) 0)) (let std/char/exclamation-mark (get (string 33) 0)) (let std/char/minus (get (string 45) 0)) (let std/char/plus (get (string 43) 0)) (let std/char/equal (get (string 61) 0)) (let std/char/asterix (get (string 42) 0)) (let std/char/ampersand (get (string 38) 0)) (let std/char/at (get (string 64) 0)) (let std/char/backtick (get (string 96) 0)) (let std/char/carriage-return (char 13)) (let nl std/char/new-line) (let sp std/char/space) (let ep std/char/empty) (let dq std/char/double-quote) (let sq std/char/single-quote) (let bt std/char/backtick) (let cr std/char/carriage-return) (let std/dec/floor (lambda n (-. n (mod. n 1.0)))) (let std/dec/ceil (lambda n (do (let sign (if (>=. n 0.0) 1 -1)) (let absn (if (>=. n 0.0) n (-. n))) (let frac (mod. absn 1.0)) (let intpart (-. absn frac)) (cond (=. n 0.0) n (if (= sign 1) (+. intpart 1.0) (-. intpart)))))) (let std/vector/length (lambda xs (length xs))) (let std/vector/get (lambda xs i (get xs i))) (let get/default (lambda xs i def (if (< i (length xs)) (get xs i) def))) (let std/vector/2d/length std/vector/length) (let std/vector/2d/get get) (let std/vector/2d/get/default get/default) (let std/vector/pop! (lambda xs (pop! xs))) (let std/vector/set! (lambda xs i x (set! xs i x))) (let std/vector/swap! (lambda xs i j (do (let temp (get xs i)) (set! xs i (get xs j)) (set! xs j temp)))) (let std/vector/push! (lambda xs x (do (set! xs (length xs) x) xs))) (let std/vector/pop-and-get! (lambda xs (do (let out (get xs (- (length xs) 1))) (pop! xs) out))) (let std/vector/push-and-get! (lambda xs x (do (set! xs (length xs) x) x))) (let std/vector/update! (lambda xs i value (do (set! xs i value) xs))) (let std/vector/tail! (lambda xs (do (pop! xs) xs))) (let std/vector/append! (lambda xs x (do (std/vector/push! xs x) xs))) (let std/vector/at (lambda xs i (if (< i 0) (get xs (+ (length xs) i)) (get xs i)))) (let std/vector/first (lambda xs (get xs 0))) (let std/vector/second (lambda xs (get xs 1))) (let std/vector/third (lambda xs (get xs 3))) (let std/vector/last (lambda xs (get xs (- (length xs) 1)))) (let box (lambda value (vector value))) (let set (lambda vrbl x (set! vrbl 0 x))) (let =! (lambda vrbl x (set! vrbl 0 x))) (let true? (lambda vrbl (if (get vrbl) true false))) (let false? (lambda vrbl (if (get vrbl) false true))) (let Bool->Int (lambda x (if (=? x true) 1 0))) (let Bool->Char (lambda x (if (=? x true) (char 49) (char 48)))) (let Char->Int (lambda x (if (>=# x std/char/empty) (as x Int) 0))) (let Char->Bool (lambda x (if (or (=# x std/char/empty) (=# x (char 48))) false true))) (let Int->Bool (lambda x (cond (<= x 0) false (>= x 1) true false))) (let Int->Char (lambda x (if (>= x 0) (as x Char) std/char/empty))) (let std/char/digit? (lambda ch (and (>=# ch (char 48)) (<=# ch (char 57))))) (let std/char/upper (lambda ch (if (and (>=# ch (char 97)) (<=# ch (char 122))) (-# ch std/char/space) ch))) (let std/char/lower (lambda ch (if (and (>=# ch (char 65)) (<=# ch (char 90))) (+# ch std/char/space) ch))) (let std/dec/safe? (lambda value (and (>=. value const/dec/min-safe) (<=. value const/dec/max-safe)))) (let std/dec/get-safe (lambda vrbl (if (std/dec/safe? (&get vrbl)) (&get vrbl) Dec))) (let int (lambda value (if (std/int/safe? value) (vector value) (vector 0)))) (let dec (lambda value (if (std/dec/safe? value) (vector value) (vector 0.0)))) (let bool (lambda value (vector (=? value true)))) (let std/int/safe? (lambda value (and (>= value const/int/min-safe) (<= value const/int/max-safe)))) (let std/int/get-safe (lambda vrbl (if (std/int/safe? (&get vrbl)) (&get vrbl) Int))) (let std/fn/apply/0 (lambda fn (fn))) (let std/fn/apply/1 (lambda x fn (fn x))) (let std/fn/apply/2 (lambda x y fn (fn x y))) (let std/fn/apply/3 (lambda x y z fn (fn x y z))) (let std/fn/apply/4 (lambda a b c d fn (fn a b c d))) (let std/fn/apply/5 (lambda a b c d e fn (fn a b c d e))) (let std/fn/apply/6 (lambda a b c d e f fn (fn a b c d e f))) (let std/fn/combinator/1 (lambda a x (a x))) (let std/fn/combinator/2 (lambda a b x (a (b x)))) (let std/fn/combinator/3 (lambda a b c x (a (b (c x))))) (let std/fn/combinator/4 (lambda a b c d x (a (b (c (d x)))))) (let std/fn/combinator/5 (lambda a b c d e x (a (b (c (d (e x))))))) (let std/fn/combinator/6 (lambda a b c d e f x (a (b (c (d (e (f x)))))))) (let std/fn/combinator/7 (lambda a b c d e f g x (a (b (c (d (e (f (g x))))))))) (let std/fn/combinator/8 (lambda a b c d e f g h x (a (b (c (d (e (f (g (h x)))))))))) (let std/fn/combinator/9 (lambda a b c d e f g h i x (a (b (c (d (e (f (g (h (i x))))))))))) (let std/fn/rev/combinator/1 (lambda a x (a x))) (let std/fn/rev/combinator/2 (lambda a b x (b (a x)))) (let std/fn/rev/combinator/3 (lambda a b c x (c (b (a x))))) (let std/fn/rev/combinator/4 (lambda a b c d x (d (c (b (a x)))))) (let std/fn/rev/combinator/5 (lambda a b c d e x (e (d (c (b (a x))))))) (let std/fn/rev/combinator/6 (lambda a b c d e f x (f (e (d (c (b (a x)))))))) (let std/fn/rev/combinator/7 (lambda a b c d e f g x (g (f (e (d (c (b (a x))))))))) (let std/fn/rev/combinator/8 (lambda a b c d e f g h x (h (g (f (e (d (c (b (a x)))))))))) (let std/fn/rev/combinator/9 (lambda a b c d e f g h i x (i (h (g (f (e (d (c (b (a x))))))))))) (let std/fn/combinator/i (lambda x x)) (let std/fn/combinator/k (lambda x y x)) (let std/fn/combinator/ki (lambda x y y)) (let std/fn/combinator/w (lambda f x (f x x))) (let std/fn/combinator/b (lambda f g x (f (g x)))) (let std/fn/combinator/c (lambda f x y (f y x))) (let std/fn/combinator/s (lambda f g x (f x (g x)))) (let std/fn/combinator/d (lambda f g x y (f x (g y)))) (let std/fn/combinator/b1 (lambda f g x y (f (g x y)))) (let std/fn/combinator/psi (lambda f g x y (f (g x) (g y)))) (let std/fn/combinator/phi (lambda f g h x (g (f x) (h x)))) (let I/comb std/fn/combinator/i) (let K/comb std/fn/combinator/k) (let KI/comb std/fn/combinator/ki) (let W/comb std/fn/combinator/w) (let B/comb std/fn/combinator/b) (let C/comb std/fn/combinator/c) (let S/comb std/fn/combinator/s) (let D/comb std/fn/combinator/d) (let B1/comb std/fn/combinator/b1) (let PSI/comb std/fn/combinator/psi) (let PHI/comb std/fn/combinator/phi) (let std/fn/const (lambda x _ x)) (let std/fn/return 1) (let std/fn/push 2) (let std/fn/none 0) (let std/fn/rec (lambda init-frame handler (do (let stack (vector init-frame)) (let result (vector (vector))) (while (not (std/vector/empty? stack)) (do (let frame (pull! stack)) (let action (handler frame)) (cond (= (fst action) std/fn/return) (do (set! result 0 (snd action)) nil) (= (fst action) std/fn/push) (do (loop 0 (length (snd action)) (lambda i (push! stack (get (snd action) i)))) nil) nil))) (get result)))) (let Rec/return std/fn/return) (let Rec/push std/fn/push) (let Rec/none std/fn/none) (let Rec std/fn/rec) (let std/vector/empty? (lambda xs (= (length xs) 0))) (let std/vector/empty! (lambda xs (if (std/vector/empty? xs) xs (do (loop 0 (length xs) (lambda _ (pop! xs))) xs)))) (let std/vector/not-empty? (lambda xs (not (= (length xs) 0)))) (let std/vector/in-bounds? (lambda xs index (and (< index (length xs)) (>= index 0)))) (let std/vector/for (lambda xs fn (do (mut i 0) (let len (length xs)) (while (< i len) (do (fn (get xs i)) (alter! i (+ i 1))))))) (let std/vector/for/i (lambda xs fn (do (mut i 0) (let len (length xs)) (while (< i len) (do (fn (get xs i) i) (alter! i (+ i 1))))))) (let std/vector/filter (lambda xs fn? (if (std/vector/empty? xs) xs (do (let out (vector)) (mut i 0) (while (< i (length xs)) (do (let x (get xs i)) (if (fn? x) (set! out (length out) x)) (alter! i (+ i 1)))) out)))) (let std/vector/filter/i (lambda xs fn? (if (std/vector/empty? xs) xs (do (let out (vector)) (mut i 0) (while (< i (length xs)) (do (let x (get xs i)) (if (fn? x i) (set! out (length out) x)) (alter! i (+ i 1)))) out)))) (let std/vector/reduce (lambda xs fn initial (do (mut out initial) (mut i 0) (while (< i (length xs)) (do (alter! out (fn out (get xs i))) (alter! i (+ i 1)))) out))) (let std/vector/reduce/i (lambda xs fn initial (do (mut out initial) (mut i 0) (while (< i (length xs)) (do (alter! out (fn out (get xs i) i)) (alter! i (+ i 1)))) out))) (let std/vector/map (lambda xs fn (if (std/vector/empty? xs) (vector) (do (let out (vector)) (mut i 0) (while (< i (length xs)) (do (set! out (length out) (fn (get xs i))) (alter! i (+ i 1)))) out)))) (let std/vector/map/i (lambda xs fn (if (std/vector/empty? xs) (vector) (do (let out (vector)) (mut i 0) (while (< i (length xs)) (do (set! out (length out) (fn (get xs i) i)) (alter! i (+ i 1)))) out)))) (let std/vector/reduce/until (lambda xs fn fn? initial (do (mut out initial) (mut placed false) (mut i 0) (let len (length xs)) (while (and (not placed) (< i len)) (do (let x (get xs i)) (let a out) (unless (fn? a x) (alter! out (fn a x)) (alter! placed true)) (alter! i (+ i 1)))) out))) (let std/vector/reduce/until/i (lambda xs fn fn? initial (do (mut out initial) (mut placed false) (mut i 0) (let len (length xs)) (while (and (not placed) (< i len)) (do (let idx i) (let x (get xs idx)) (let a out) (unless (fn? a x idx) (alter! out (fn a x idx)) (alter! placed true)) (alter! i (+ i 1)))) out))) (let std/vector/for/until (lambda xs fn fn? (do (mut placed false) (mut i 0) (let len (length xs)) (while (and (not placed) (< i len)) (do (let x (get xs i)) (unless (fn? x) (do (fn x) nil) (alter! placed true)) (alter! i (+ i 1))))))) (let std/vector/for/until/i (lambda xs fn fn? (do (mut placed false) (mut i 0) (let len (length xs)) (while (and (not placed) (< i len)) (do (let idx i) (let x (get xs idx)) (unless (fn? x idx) (do (fn x idx) nil) (alter! placed true)) (alter! i (+ i 1))))))) (let std/vector/map/until (lambda xs fn fn? (do (let out (vector)) (mut placed false) (mut i 0) (let len (length xs)) (while (and (not placed) (< i len)) (do (let x (get xs i)) (unless (fn? x) (do (push! out (fn x)) nil) (alter! placed true)) (alter! i (+ i 1)))) out))) (let std/vector/map/until/i (lambda xs fn fn? (do (let out (vector)) (mut placed false) (mut i 0) (let len (length xs)) (while (and (not placed) (< i len)) (do (let idx i) (let x (get xs idx)) (unless (fn? x idx) (do (push! out (fn x idx)) nil) (alter! placed true)) (alter! i (+ i 1)))) out))) (let std/vector/int/range (lambda start end (do (let out (vector start)) (loop (+ start 1) (+ end 1) (lambda i (set! out (length out) i))) out))) (let std/vector/dec/range (lambda start end (do (let out (vector (Int->Dec start))) (loop (+ start 1) (+ end 1) (lambda i (set! out (length out) (Int->Dec i)))) out))) (let std/vector/dec/ones (lambda n (do (let out (vector)) (loop 0 n (lambda i (set! out (length out) 1.0))) out))) (let std/vector/dec/zeroes (lambda n (do (let out (vector)) (loop 0 n (lambda i (set! out (length out) 0.0))) out))) (let std/vector/int/ones (lambda n (do (let out (vector)) (loop 0 n (lambda i (set! out (length out) 1))) out))) (let std/vector/int/zeroes (lambda n (do (let out (vector)) (loop 0 n (lambda i (set! out (length out) 0))) out))) (let std/vector/bool/true (lambda n (do (let out (vector)) (loop 0 n (lambda i (set! out (length out) true))) out))) (let std/vector/bool/false (lambda n (do (let out (vector)) (loop 0 n (lambda i (set! out (length out) false))) out))) (let std/vector/3d/int/range (lambda s w h (do (mut i s) (let matrix (vector)) (mut j 0) (while (< j w) (do (mut k 0) (let current (vector)) (push! matrix current) (while (< k h) (do (push! current i) (alter! i (+ i 1)) (alter! k (+ k 1)))) (alter! j (+ j 1)))) matrix))) (let std/vector/2d/int/range std/vector/int/range) (let std/vector/2d/dec/range std/vector/dec/range) (let std/vector/char/blanks (lambda n (do (let out (vector std/char/empty)) (loop 1 n (lambda i (set! out (length out) std/char/empty))) out))) (let std/vector/int/all-equal? (lambda xs (do (let x (get xs 0)) (std/vector/every? xs (lambda y (= y x)))))) (let std/vector/dec/all-equal? (lambda xs (do (let x (get xs 0)) (std/vector/every? xs (lambda y (=. y x)))))) (let std/vector/char/all-equal? (lambda xs (do (let x (get xs 0)) (std/vector/every? xs (lambda y (=# y x)))))) (let std/vector/bool/all-equal? (lambda xs (do (let x (get xs 0)) (std/vector/every? xs (lambda y (=? y x)))))) (let all-equal/int? std/vector/int/all-equal?) (let all-equal/dec? std/vector/dec/all-equal?) (let all-equal/char? std/vector/char/all-equal?) (let all-equal/bool? std/vector/bool/all-equal?) (let std/vector/count-of (lambda xs fn? (length (std/vector/filter xs fn?)))) (let std/vector/int/count (lambda xs item (std/vector/count-of xs (lambda x (= x item))))) (let std/vector/dec/count (lambda xs item (std/vector/count-of xs (lambda x (=. x item))))) (let std/vector/char/count (lambda xs item (std/vector/count-of xs (lambda x (=# x item))))) (let std/vector/bool/count (lambda xs item (std/vector/count-of xs (lambda x (=? x item))))) (let std/vector/2d/count-of std/vector/count-of) (let std/vector/2d/int/count std/vector/int/count) (let std/vector/2d/char/count std/vector/char/count) (let std/vector/2d/bool/count std/vector/bool/count) (let std/vector/3d/count-of (lambda xs fn? (<| xs (std/vector/map (lambda ys (std/vector/2d/count-of ys fn?))) (std/vector/int/sum)))) (let std/vector/3d/int/count (lambda xs x (<| xs (std/vector/map (lambda ys (std/vector/2d/int/count ys x))) (std/vector/int/sum)))) (let std/vector/3d/char/count (lambda xs x (<| xs (std/vector/map (lambda ys (std/vector/2d/char/count ys x))) (std/vector/int/sum)))) (let std/vector/3d/bool/count (lambda xs x (<| xs (std/vector/map (lambda ys (std/vector/2d/bool/count ys x))) (std/vector/int/sum)))) (let std/vector/cons (lambda a b (do (let lena (length a)) (let lenb (length b)) (let out (vector)) (mut i 0) (while (< i lena) (do (set! out (length out) (get a i)) (alter! i (+ i 1)))) (mut j 0) (while (< j lenb) (do (set! out (length out) (get b j)) (alter! j (+ j 1)))) out))) (let std/vector/cons! (lambda a b (if (and (std/vector/empty? a) (std/vector/empty? b)) a (do (loop 0 (length b) (lambda i (set! a (length a) (get b i)))) a)))) (let std/vector/concat (lambda xs (std/vector/reduce xs std/vector/cons (vector)))) (let std/vector/concat! (lambda xs os (std/vector/reduce os std/vector/cons! xs))) (let std/vector/every? (lambda xs predicate? (do (mut i 0) (let len (length xs)) (while (and (< i len) (predicate? (get xs i))) (alter! i (+ i 1))) (not (> len i))))) (let std/vector/some? (lambda xs predicate? (do (mut i 0) (let len (length xs)) (while (and (< i len) (not (predicate? (get xs i)))) (alter! i (+ i 1))) (or (= len 0) (> len i))))) (let std/vector/every/i? (lambda xs predicate? (do (mut i 0) (let len (length xs)) (while (and (< i len) (predicate? (get xs i) i)) (alter! i (+ i 1))) (not (> len i))))) (let std/vector/some/i? (lambda xs predicate? (do (mut i 0) (let len (length xs)) (while (and (< i len) (not (predicate? (get xs i) i))) (alter! i (+ i 1))) (or (= len 0) (> len i))))) (let std/vector/cartesian-product (lambda a b (std/vector/reduce a (lambda p x (std/vector/cons p (std/vector/map b (lambda y (tuple x y))))) (vector)))) (let std/int/gcd (lambda a b (do (mut A a) (mut B b) (while (> B 0) (do (let a A) (let b B) (alter! A b) (alter! B (mod a b)))) A))) (let std/int/lcm (lambda a b (/ (* a b) (std/int/gcd a b)))) (let std/int/bit/set? (lambda n pos (= (& n (<< 1 pos)) 0))) (let std/int/bit/set (lambda n pos (| n (<< 1 pos)))) (let std/int/bit/clear (lambda n pos (& n (~ (<< 1 pos))))) (let std/int/bit/power-of-two (lambda n (<< 2 (- n 1)))) (let std/int/bit/odd? (lambda n (= (& n 1) 1))) (let std/int/bit/even? (lambda n (= (& n 1) 0))) (let std/int/bit/average (lambda a b (>> (+ a b) 1))) (let std/int/bit/flag-flip (lambda x (- 1 (* x x)))) (let std/int/bit/toggle (lambda n a b (^ (^ a b) n))) (let std/int/bit/same-sign? (lambda a b (>= (^ a b) 0))) (let std/int/bit/max (lambda a b (- a (& (- a b) (>> (- a b) 31))))) (let std/int/bit/min (lambda a b (- a (& (- a b) (>> (- b a) 31))))) (let std/int/bit/equal? (lambda a b (< (^ a b) 1))) (let std/int/bit/modulo (lambda numerator divisor (& numerator (- divisor 1)))) (let std/int/bit/n-one? (lambda N nth (not (= (& N (<< 1 nth)) 0)))) (let std/int/bit/largest-power (lambda N (do (let N1 (| N (>> N 1))) (let N2 (| N1 (>> N1 2))) (let N3 (| N2 (>> N2 4))) (let N4 (| N3 (>> N3 8))) (>> (+ N4 1) 1)))) (let std/int/abs (lambda n (- (^ n (>> n 31)) (>> n 31)))) (let std/dec/abs (lambda n (if (<. n 0.0) (*. n -1.0) n))) (let std/int/positive? (lambda x (> x 0))) (let std/int/negative? (lambda x (< x 0))) (let std/int/invert (lambda x (- x))) (let std/int/zero? (lambda x (= x 0))) (let std/int/one? (lambda x (= x 1))) (let std/int/negative-one? (lambda x (= x -1))) (let std/int/divisible? (lambda a b (= (mod a b) 0))) (let std/int/floor/div (lambda a b (/ a b))) (let std/int/ceil/div (lambda a b (/ (+ a b -1) b))) (let std/dec/positive? (lambda x (>. x 0.0))) (let std/dec/negative? (lambda x (<. x 0.0))) (let std/dec/invert (lambda x (-. x))) (let std/dec/zero? (lambda x (=. x 0.0))) (let std/dec/one? (lambda x (=. x 1.0))) (let std/dec/negative-one? (lambda x (=. x -1.0))) (let std/dec/divisible? (lambda a b (=. (mod. a b) 0.0))) (let std/int/square (lambda x (* x x))) (let std/int/even? (lambda x (= (mod x 2) 0))) (let std/int/odd? (lambda x (not (= (mod x 2) 0)))) (let std/dec/even? (lambda x (=. (mod. x 2.0) 0.0))) (let std/dec/odd? (lambda x (not (=. (mod. x 2.0) 0.0)))) (let std/vector/int/sum (lambda xs (std/vector/reduce xs (lambda a b (+ a b)) 0))) (let std/vector/dec/sum (lambda xs (std/vector/reduce xs (lambda a b (+. a b)) 0.0))) (let std/vector/int/product (lambda xs (std/vector/reduce xs (lambda a b (* a b)) 1))) (let std/vector/dec/product (lambda xs (std/vector/reduce xs (lambda a b (*. a b)) 1.0))) (let std/int/mul (lambda a b (* a b))) (let std/int/add (lambda a b (+ a b))) (let std/int/div (lambda a b (/ a b))) (let std/int/sub (lambda a b (- a b))) (let std/int/euclidean-mod (lambda a b (mod (+ (mod a b) b) b))) (let std/int/euclidean-distance (lambda x1 y1 x2 y2 (do (let a (- x1 x2)) (let b (- y1 y2)) (std/int/sqrt (+ (* a a) (* b b)))))) (let std/int/manhattan-distance (lambda x1 y1 x2 y2 (+ (std/int/abs (- x2 x1)) (std/int/abs (- y2 y1))))) (let std/int/chebyshev-distance (lambda x1 y1 x2 y2 (std/int/max (std/int/abs (- x2 x1)) (std/int/abs (- y2 y1))))) (let std/int/max (lambda a b (if (> a b) a b))) (let std/int/min (lambda a b (if (< a b) a b))) (let std/dec/max (lambda a b (if (>. a b) a b))) (let std/dec/min (lambda a b (if (<. a b) a b))) (let std/vector/int/maximum (lambda xs (cond (std/vector/empty? xs) Int (= (length xs) 1) (get xs 0) (std/vector/reduce xs std/int/max (get xs 0))))) (let std/vector/int/minimum (lambda xs (cond (std/vector/empty? xs) Int (= (length xs) 1) (get xs 0) (std/vector/reduce xs std/int/min (get xs 0))))) (let std/vector/dec/maximum (lambda xs (cond (std/vector/empty? xs) Dec (= (length xs) 1) (get xs 0) (std/vector/reduce xs std/dec/max (get xs 0))))) (let std/vector/dec/minimum (lambda xs (cond (std/vector/empty? xs) Dec (= (length xs) 1) (get xs 0) (std/vector/reduce xs std/dec/min (get xs 0))))) (let std/dec/average (lambda x y (/. (+. x y) 2.0))) (let std/int/average (lambda x y (/ (+ x y) 2))) (let std/vector/int/mean (lambda xs (/ (std/vector/int/sum xs) (length xs)))) (let std/vector/dec/mean (lambda xs (/. (std/vector/dec/sum xs) (Int->Dec (length xs))))) (let std/vector/int/median (lambda xs (do (let len (length xs)) (let half (/ len 2)) (if (std/int/odd? len) (get xs half) (/ (+ (get xs (- half 1)) (get xs half)) 2))))) (let std/vector/dec/median (lambda xs (do (let len (Int->Dec (length xs))) (let half (/. len 2.0)) (let mid (Dec->Int half)) (if (std/dec/odd? len) (get xs mid) (/. (+. (get xs (Dec->Int (-. half 1.0))) (get xs mid)) 2.0))))) (let std/int/normalize (lambda value min max (* (- value min) (/ (- max min))))) (let std/int/linear-interpolation (lambda a b n (+ (* (- 1 n) a) (* n b)))) (let std/int/gauss-sum (lambda n (/ (* n (+ n 1)) 2))) (let std/int/gauss-sum-sequance (lambda a b (/ (* (+ a b) (+ (- b a) 1)) 2))) (let std/int/clamp (lambda x limit (if (> x limit) limit x))) (let std/int/clamp-range (lambda x start end (cond (> x end) end (< x start) start x))) (let std/int/between? (lambda v min max (and (> v min) (< v max)))) (let std/int/overlap? (lambda v min max (and (>= v min) (<= v max)))) (let std/dec/clamp (lambda x limit (if (>. x limit) limit x))) (let std/dec/clamp-range (lambda x start end (cond (>. x end) end (<. x start) start x))) (let std/dec/between? (lambda v min max (and (>. v min) (<. v max)))) (let std/dec/overlap? (lambda v min max (and (>=. v min) (<=. v max)))) (let std/int/sqrt (lambda n (do (mut low 0) (mut high n) (mut mid 0) (mut res 0) (while (<= low high) (do (alter! mid (+ low (/ (- high low) 2))) (if (<= mid 0) (do (alter! res mid) (alter! low (+ mid 1))) (if (<= mid (/ n mid)) (do (alter! res mid) (alter! low (+ mid 1))) (alter! high (- mid 1)))))) res))) (let std/int/expt (lambda base exp (do (if (< exp 0) 0 (do (mut result 1) (mut b base) (mut e exp) (while (> e 0) (do (if (= (mod e 2) 1) (alter! result (* result b))) (alter! b (* b b)) (alter! e (/ e 2)))) result))))) (let iexpt std/int/expt) (let std/dec/sqrt (lambda n (do (&mut low 0.0) (&mut high n) (&mut mid 0.0) (&mut i 0.0) (while (<. (&get i) 100.0) (do (&alter! mid (/. (+. (&get low) (&get high)) 2.0)) (if (<=. (*. (&get mid) (&get mid)) n) (&alter! low (&get mid)) (&alter! high (&get mid))) (&alter! i (+. (&get i) 1.0)))) (&get low)))) (let std/dec/expt (lambda base exp (do (&mut res 1.0) (&mut b base) (&mut e exp) (while (>=. (&get e) 1.0) (do (if (=. (mod. (floor (&get e)) 2.0) 1.0) (&alter! res (*. (&get res) (&get b)))) (&alter! b (*. (&get b) (&get b))) (&alter! e (/. (floor (&get e)) 2.0)))) (&alter! b base) (&alter! e (-. exp (floor exp))) (&mut root (std/dec/sqrt (&get b))) (&mut frac 0.5) (&mut i 0.0) (while (<. (&get i) 22.0) (do (if (>=. (&get e) (&get frac)) (do (&alter! res (*. (&get res) (&get root))) (&alter! e (-. (&get e) (&get frac))))) (&alter! root (std/dec/sqrt (&get root))) (&alter! frac (/. (&get frac) 2.0)) (&alter! i (+. (&get i) 1.0)))) (&get res)))) (let std/int/delta (lambda a b (std/int/abs (- a b)))) (let std/dec/delta (lambda a b (std/dec/abs (-. a b)))) (let std/vector/map/adjacent (lambda xs fn (if (std/vector/empty? xs) (vector) (do (do (let out (vector)) (loop 1 (length xs) (lambda i (std/vector/push! out (fn (get xs (- i 1)) (get xs i))))) out))))) (let std/vector/zipper (lambda a b (do (mut i 1) (let len (length a)) (let out (vector (vector (get a 0) (get b 0)))) (while (< i len) (do (set! out (length out) (vector (get a i) (get b i))) (alter! i (+ i 1)))) out))) (let std/vector/zip (lambda xs (std/vector/zipper (std/vector/first xs) (std/vector/second xs)))) (let std/vector/unzip (lambda xs (vector (std/vector/map xs std/vector/first) (std/vector/map xs std/vector/second)))) (let std/vector/tuple/zipper (lambda a b (do (mut i 1) (let len (length a)) (let out (vector (tuple (get a 0) (get b 0)))) (while (< i len) (do (set! out (length out) (tuple (get a i) (get b i))) (alter! i (+ i 1)))) out))) (let std/vector/tuple/zip (lambda xs (std/vector/tuple/zipper (fst xs) (snd xs)))) (let std/vector/tuple/unzip (lambda xs (tuple (std/vector/map xs (lambda x (fst x))) (std/vector/map xs (lambda x (snd x)))))) (let std/vector/tuple/zip-with (lambda a b f (do (let out (vector)) (mut i 0) (let len (length a)) (while (< i len) (do (set! out (length out) (f (get a i) (get b i))) (alter! i (+ i 1)))) out))) (let std/vector/rest (lambda xs start (if (std/vector/empty? xs) xs (do (let end (length xs)) (let bounds (- end start)) (let out (vector)) (mut i 0) (while (< i bounds) (do (set! out (length out) (get xs (+ start i))) (alter! i (+ i 1)))) out)))) (let std/vector/slice (lambda xs start end (if (std/vector/empty? xs) xs (do (let bounds (- end start)) (let out (vector)) (mut i 0) (while (< i bounds) (do (set! out (length out) (get xs (+ start i))) (alter! i (+ i 1)))) out)))) (let std/vector/drop (lambda xs start (if (std/vector/empty? xs) xs (do (let end (length xs)) (let bounds (- end start)) (let out (vector)) (mut i 0) (while (< i bounds) (do (set! out (length out) (get xs (+ start i))) (alter! i (+ i 1)))) out)))) (let std/vector/drop/last (lambda xs end (if (std/vector/empty? xs) xs (do (let bounds (- (length xs) end)) (let out (vector)) (mut i 0) (while (< i bounds) (do (set! out (length out) (get xs i)) (alter! i (+ i 1)))) out)))) (let std/vector/take (lambda xs end (if (std/vector/empty? xs) xs (do (let out (vector)) (mut i 0) (while (< i end) (do (set! out (length out) (get xs i)) (alter! i (+ i 1)))) out)))) (let std/vector/take/last (lambda xs start (if (std/vector/empty? xs) xs (do (let out (vector)) (let len (length xs)) (mut i (- len start)) (while (< i len) (do (set! out (length out) (get xs i)) (alter! i (+ i 1)))) out)))) (let std/vector/take/while (lambda xs fn? (do (let out (vector)) (mut i 0) (while (and (< i (length xs)) (fn? (get xs i))) (do (push! out (get xs i)) (++ i))) out))) (let std/vector/drop/while (lambda xs fn? (do (mut i 0) (while (and (< i (length xs)) (fn? (get xs i))) (++ i)) (cdr xs i)))) (let std/vector/reverse (lambda xs (if (std/vector/empty? xs) xs (do (let out (vector)) (let len (length xs)) (mut i 0) (while (< i len) (do (set! out (length out) (get xs (- len i 1))) (alter! i (+ i 1)))) out)))) (let std/vector/reverse! (lambda xs (loop 0 (/ (length xs) 2) (lambda i (std/vector/swap! xs i (- (length xs) i 1)))))) (let std/vector/find-index (lambda xs fn? (do (mut i 0) (mut index -1) (let len (length xs)) (while (and (< i len) (= index -1)) (if (fn? (get xs i)) (alter! index i) (alter! i (+ i 1)))) index))) (let std/vector/buckets (lambda size (do (let out (vector (vector))) (mut i 1) (while (< i size) (do (set! out (length out) (vector)) (alter! i (+ i 1)))) out))) (let std/vector/char/equal? (lambda a b (do (let len-a (length a)) (let len-b (length b)) (if (<> len-a len-b) false (do (mut i 0) (mut same true) (while (and same (< i len-a)) (do (if (not (=# (get a i) (get b i))) (alter! same false) nil) (alter! i (+ i 1)))) same))))) (let std/vector/char/greater? (lambda a b (do (let len-a (length a)) (let len-b (length b)) (let min-len (if (< len-a len-b) len-a len-b)) (mut i 0) (mut decided false) (mut out false) (while (and (not decided) (< i min-len)) (do (let ca (get a i)) (let cb (get b i)) (if (=# ca cb) (alter! i (+ i 1)) (do (alter! out (># ca cb)) (alter! decided true))))) (if decided out (> len-a len-b))))) (let std/vector/char/lesser? (lambda a b (do (let len-a (length a)) (let len-b (length b)) (let min-len (if (< len-a len-b) len-a len-b)) (mut i 0) (mut decided false) (mut out false) (while (and (not decided) (< i min-len)) (do (let ca (get a i)) (let cb (get b i)) (if (=# ca cb) (alter! i (+ i 1)) (do (alter! out (<# ca cb)) (alter! decided true))))) (if decided out (< len-a len-b))))) (let std/vector/char/match? std/vector/char/equal?) (let std/vector/char/greater-or-equal? (lambda A B (or (std/vector/char/equal? A B) (std/vector/char/greater? A B)))) (let std/vector/char/lesser-or-equal? (lambda A B (or (std/vector/char/equal? A B) (std/vector/char/lesser? A B)))) (let std/vector/char/negative? (lambda str (=# (std/vector/first str) std/char/minus))) (let std/vector/partition (lambda xs n (if (= n (length xs)) (vector xs) (do (let a (vector)) (mut i 0) (let len (length xs)) (while (< i len) (do (if (= (mod i n) 0) (set! a (length a) (vector (get xs i))) (set! (std/vector/at a -1) (length (std/vector/at a -1)) (get xs i))) (alter! i (+ i 1)))) a)))) (let std/vector/sort-partition! (lambda arr start end fn (do (let pivot (get arr end)) (mut i (- start 1)) (mut j start) (while (< j end) (do (if (fn (get arr j) pivot) (do (alter! i (+ i 1)) (std/vector/swap! arr i j) nil)) (alter! j (+ j 1)))) (std/vector/swap! arr (+ i 1) end) (+ i 1)))) (let std/vector/sort! (lambda arr fn (do (let stack (vector)) (push! stack 0) (push! stack (- (length arr) 1)) (while (> (length stack) 0) (do (let end (get stack (- (length stack) 1))) (pop! stack) (let start (get stack (- (length stack) 1))) (pop! stack) (if (< start end) (do (let pivot-index (std/vector/sort-partition! arr start end fn)) (push! stack start) (push! stack (- pivot-index 1)) (push! stack (+ pivot-index 1)) (push! stack end) nil)))) arr))) (let std/vector/safe-sort! (lambda v fn (do (let init-frame (tuple (tuple 0 (- (length v) 1)) v)) (let handler (lambda (tuple (tuple low high) vec) (if (>= low high) (tuple std/fn/none (vector)) (do (let pivot (get vec high)) (&mut i low) (&mut j low) (while (< (&get j) high) (do (if (fn (get vec (&get j)) pivot) (do (std/vector/swap! vec (&get i) (&get j)) (&alter! i (+ (&get i) 1))) nil) (&alter! j (+ (&get j) 1)))) (std/vector/swap! vec (&get i) high) (let p (&get i)) (tuple std/fn/push (vector (tuple (tuple low (- p 1)) vec) (tuple (tuple (+ p 1) high) vec))))))) (std/fn/rec init-frame handler) v))) (let std/vector/sliding-window (lambda xs size (cond (std/vector/empty? xs) (vector) (= size (length xs)) (vector xs) (std/vector/reduce/i xs (lambda a b i (if (> (+ i size) (length xs)) a (std/vector/cons a (vector (std/vector/slice xs i (+ i size)))))) (vector))))) (let std/vector/flat-one (lambda xs (cond (std/vector/empty? xs) (vector) (= (length xs) 1) (get xs) (std/vector/reduce xs (lambda a b (std/vector/cons a b)) (vector))))) (let std/vector/flat/length (lambda matrix (length (std/vector/flat-one matrix)))) (let std/convert/char->digit (lambda digit (if (<# digit (char 48)) 0 (- (as digit Int) (as (char 48) Int))))) (let std/convert/chars->digits (lambda digits (std/vector/map digits std/convert/char->digit))) (let std/convert/digit->char (lambda digit (if (< digit 0) (char 48) (+# (as digit Char) (char 48))))) (let std/convert/digits->chars (lambda digits (std/vector/map digits std/convert/digit->char))) (let std/convert/bool->int (lambda x (if (=? x true) 1 0))) (let std/convert/int->bool (lambda x (if (= x 0) false true))) (let std/convert/vector->string (lambda xs delim (std/vector/reduce/i xs (lambda a b i (if (> i 0) (std/vector/cons (std/vector/append! a delim) b) b)) (string)))) (let std/convert/string->vector (lambda str ch (<| str (std/vector/reduce (lambda a b (do (let prev (std/vector/at a -1)) (if (std/vector/char/equal? (vector b) (vector ch)) (set! a (length a) (vector)) (set! prev (length prev) b)) a)) (vector (vector))) (std/vector/map (lambda x (std/convert/vector->string (vector x) std/char/empty)))))) (let std/convert/positive-or-negative-digits->integer (lambda digits-with-sign (do (let std/int/negative? (< (std/vector/first digits-with-sign) 0)) (let digits (if std/int/negative? (std/vector/map digits-with-sign std/int/abs) digits-with-sign)) (mut num 0) (mut base (/ (std/int/expt 10 (length digits)) 10)) (mut i 0) (while (< i (length digits)) (do (alter! num (+ num (* base (get digits i)))) (alter! base (/ base 10)) (alter! i (+ i 1)))) (alter! num (* num (if std/int/negative? -1 1))) num))) (let std/convert/chars->positive-or-negative-digits (lambda chars (do (integer current-sign 1) (<| chars (std/vector/reduce (lambda a ch (do (if (=# ch std/char/minus) (set current-sign -1) (do (std/vector/push! a (* (get current-sign) (std/convert/char->digit ch))) (set current-sign 1))) a)) (vector)))))) (let std/convert/digits->integer std/convert/positive-or-negative-digits->integer) (let std/convert/positive-or-negative-chars->integer (lambda x (<| x (std/convert/chars->positive-or-negative-digits) (std/convert/positive-or-negative-digits->integer)))) (let std/convert/chars->integer std/convert/positive-or-negative-chars->integer) (let std/convert/chars->digits/dec (lambda xs (<| xs (std/vector/reduce (lambda a ch (do (if (=# ch (char 46)) (push! a (vector)) (push! (std/vector/at a -1) (std/convert/char->digit ch))) a)) (vector (vector)))))) (let std/convert/chars->ufloat (lambda xs (do (let parts (std/convert/chars->digits/dec xs)) (let pow (std/int/expt 10 (length (get parts 1)))) (/. (Int->Dec (+ (* (std/convert/digits->integer (get parts 0)) pow) (std/convert/digits->integer (get parts 1)))) (Int->Dec pow))))) (let std/convert/chars->dec (lambda xs (if (=# (get xs 0) std/char/minus) (*. (std/convert/chars->ufloat (std/vector/slice xs 1 (length xs))) -1.0) (std/convert/chars->ufloat xs)))) (let std/convert/int->char/alphabet (lambda x offset (Int->Char (+ x (Char->Int offset))))) (let std/vector/unique-pairs (lambda xs (do (let pairs (vector)) (let len (length xs)) (mut i 0) (while (< i len) (do (mut j (+ i 1)) (while (< j len) (do (std/vector/push! pairs (vector (get xs i) (get xs j))) (alter! j (+ j 1)))) (alter! i (+ i 1)))) pairs))) (let std/vector/tuple/unique-pairs (lambda xs (do (let pairs (vector)) (let len (length xs)) (mut i 0) (while (< i len) (do (mut j (+ i 1)) (while (< j len) (do (std/vector/push! pairs (tuple (get xs i) (get xs j))) (alter! j (+ j 1)))) (alter! i (+ i 1)))) pairs))) (let std/vector/int/unique (lambda xs (if (= (length xs) 0) (vector (+ (get xs 0) 0)) (<| xs (std/vector/map (lambda x (vector (as x Char)))) (std/convert/vector->set) (std/convert/set->vector) (std/vector/map (lambda x (as (get x 0) Int))))))) (let std/vector/char/unique (lambda xs (if (= (length xs) 0) xs (<| xs (std/vector/map (lambda x (vector x))) (std/convert/vector->set) (std/convert/set->vector) (std/vector/map (lambda x (get x 0))))))) (let std/vector/3d/dimensions (lambda matrix (vector (length matrix) (length (get matrix 0))))) (let std/vector/3d/in-bounds? (lambda matrix y x (and (std/vector/in-bounds? matrix y) (std/vector/in-bounds? (get matrix y) x)))) (let std/vector/3d/set! (lambda matrix y x value (do (set! (get matrix y) x value) 0))) (let std/vector/3d/diagonal-neighborhood (vector (vector 1 -1) (vector -1 -1) (vector 1 1) (vector -1 1))) (let std/vector/3d/kernel-neighborhood (vector (vector 0 0) (vector 0 1) (vector 1 0) (vector -1 0) (vector 0 -1) (vector 1 -1) (vector -1 -1) (vector 1 1) (vector -1 1))) (let std/vector/3d/moore-neighborhood (vector (vector 0 1) (vector 1 0) (vector -1 0) (vector 0 -1) (vector 1 -1) (vector -1 -1) (vector 1 1) (vector -1 1))) (let std/vector/3d/von-neumann-neighborhood (vector (vector 1 0) (vector 0 -1) (vector 0 1) (vector -1 0))) (let std/vector/3d/adjacent (lambda xs directions y x fn (std/vector/for directions (lambda dir (do (let dy (+ (std/vector/first dir) y)) (let dx (+ (std/vector/second dir) x)) (if (std/vector/3d/in-bounds? xs dy dx) (fn (get xs dy dx) dir dy dx))))))) (let std/vector/3d/sliding-adjacent-sum (lambda xs directions y x N fn (std/vector/reduce directions (lambda a dir (do (let dy (+ (std/vector/first dir) y)) (let dx (+ (std/vector/second dir) x)) (fn a (get xs (std/int/euclidean-mod dy N) (std/int/euclidean-mod dx N))))) 0))) (let std/node/parent (lambda i (- (>> (+ i 1) 1) 1))) (let std/node/left (lambda i (+ (<< i 1) 1))) (let std/node/right (lambda i (<< (+ i 1) 1))) (let std/heap/top 0) (let std/heap/greater? (lambda heap i j fn? (=? (fn? (get heap i) (get heap j)) true))) (let std/heap/sift-up! (lambda heap fn (do (integer node (- (length heap) 1)) (letrec tail-call/std/heap/sift-up! (lambda heap (if (and (> (get node) std/heap/top) (std/heap/greater? heap (get node) (std/node/parent (get node)) fn)) (do (std/vector/swap! heap (get node) (std/node/parent (get node))) (set node (std/node/parent (get node))) (tail-call/std/heap/sift-up! heap)) heap))) (tail-call/std/heap/sift-up! heap)))) (let std/heap/sift-down! (lambda heap fn (do (integer node std/heap/top) (letrec tail-call/std/heap/sift-down! (lambda heap (if (or (and (< (std/node/left (get node)) (length heap)) (std/heap/greater? heap (std/node/left (get node)) (get node) fn)) (and (< (std/node/right (get node)) (length heap)) (std/heap/greater? heap (std/node/right (get node)) (get node) fn))) (do (let max-child (if (and (< (std/node/right (get node)) (length heap)) (std/heap/greater? heap (std/node/right (get node)) (std/node/left (get node)) fn)) (std/node/right (get node)) (std/node/left (get node)))) (std/vector/swap! heap (get node) max-child) (set node max-child) (tail-call/std/heap/sift-down! heap)) heap))) (tail-call/std/heap/sift-down! heap)))) (let std/heap/peek (lambda heap (get heap std/heap/top))) (let std/heap/push! (lambda heap value fn (do (set! heap (length heap) value) (std/heap/sift-up! heap fn) nil))) (let std/heap/pop! (lambda heap fn (do (let bottom (- (length heap) 1)) (if (> bottom std/heap/top) (do (std/vector/swap! heap std/heap/top bottom) heap) heap) (pop! heap) (std/heap/sift-down! heap fn) nil))) (let std/heap/replace! (lambda heap value fn (do (set! heap std/heap/top value) (std/heap/sift-down! heap fn) heap))) (let std/heap/empty? std/vector/empty?) (let std/heap/not-empty? std/vector/not-empty?) (let std/heap/empty! std/vector/empty!) (let std/convert/vector->heap (lambda xs fn (std/vector/reduce xs (lambda heap x (do (std/heap/push! heap x fn) heap)) (vector)))) (let std/convert/set->vector (lambda xs (std/vector/filter (std/vector/flat-one xs) std/vector/not-empty?))) (let std/convert/integer->string-base (lambda num base (if (= num 0) (string 48) (do (let neg? (< num 0)) (integer n (if neg? (* num -1) num)) (letrec tail-call/while (lambda out (if (> (get n) 0) (do (let x (mod (get n) base)) (std/vector/push! out x) (set n (/ (get n) base)) (tail-call/while out)) out))) (let str (std/convert/digits->chars (tail-call/while (vector)))) (std/vector/reverse (if neg? (std/vector/append! str std/char/dash) str)))))) (let std/convert/integer->string (lambda x (std/convert/integer->string-base x 10))) (let std/convert/vector->set (lambda xs (std/vector/reduce xs (lambda s x (do (std/vector/hash/set/add! s x) s)) (vector (vector) (vector) (vector) (vector) (vector) (vector) (vector))))) (let std/integer/dec-scaling 1000000) (let std/dec/dec-scaling 1000000.0) (let std/convert/dec->string (lambda x (if (=. (std/dec/floor x) x) (cons (std/convert/integer->string (Dec->Int x)) (string 46 48)) (do (let flip (if (<. x 0.0) -1.0 1.0)) (let exponent (std/dec/floor x)) (let mantisa (-. x exponent)) (let left (std/convert/integer->string (Dec->Int exponent))) (let right (std/convert/integer->string (Dec->Int (*. mantisa std/dec/dec-scaling flip)))) (let len (length right)) (letrec tail-call/while (lambda i (if (=# (get right (- len i)) (char 48)) (do (pop! right) (tail-call/while (+ i 1))) i))) (tail-call/while 1) (cons left (vector std/char/dot) right))))) (let std/vector/deque/new (lambda def (vector (vector def) (vector)))) (let std/vector/deque/offset-left (lambda q (* (- (length (get q 0)) 1) -1))) (let std/vector/deque/offset-right (lambda q (length (get q 1)))) (let std/vector/deque/length (lambda q (+ (length (get q 0)) (length (get q 1)) -1))) (let std/vector/deque/empty? (lambda q (= (std/vector/deque/length q) 0))) (let std/vector/deque/empty! (lambda q (do (set! q 0 (vector (get q 0 0))) (set! q 1 (vector)) q))) (let std/vector/deque/get (lambda q offset (do (let offset-index (+ offset (std/vector/deque/offset-left q))) (let index (if (< offset-index 0) (* offset-index -1) offset-index)) (if (>= offset-index 0) (get (get q 1) index) (get (get q 0) index))))) (let std/vector/deque/set! (lambda q index value (do (let offset (+ index (std/vector/deque/offset-left q))) (if (>= offset 0) (set! (get q 1) offset value) (set! (get q 0) (* offset -1) value)) q))) (let std/vector/deque/add-to-left! (lambda q item (do (let c (get q 0)) (set! c (length c) item)))) (let std/vector/deque/add-to-right! (lambda q item (do (let c (get q 1)) (set! c (length c) item)))) (let std/vector/deque/remove-from-left! (lambda q (do (let len (std/vector/deque/length q)) (if (> len 0) (cond (= len 1) (std/vector/deque/empty! q) (> (length (get q 0)) 0) (do (pop! (get q 0)) q) q) q)))) (let std/vector/deque/remove-from-right! (lambda q (do (let len (std/vector/deque/length q)) (if (> len 0) (cond (= len 1) (std/vector/deque/empty! q) (> (length (get q 1)) 0) (do (pop! (get q 1)) q) q) q)))) (let std/vector/deque/iter (lambda q fn (do (letrec tail-call/std/vector/deque/iter (lambda index bounds (do (fn (std/vector/deque/get q index)) (if (< index bounds) (tail-call/std/vector/deque/iter (+ index 1) bounds) Int)))) (tail-call/std/vector/deque/iter 0 (std/vector/deque/length q))))) (let std/vector/deque/map (lambda q fn (do (let result (std/vector/deque/new)) (let len (std/vector/deque/length q)) (let half (/ len 2)) (letrec tail-call/left/std/vector/deque/map (lambda index (do (std/vector/deque/add-to-left! result (fn (std/vector/deque/get q index))) (if (> index 0) (tail-call/left/std/vector/deque/map (- index 1)) Int)))) (tail-call/left/std/vector/deque/map (- half 1)) (letrec tail-call/right/std/vector/deque/map (lambda index bounds (do (std/vector/deque/add-to-right! result (fn (std/vector/deque/get q index))) (if (< index bounds) (tail-call/right/std/vector/deque/map (+ index 1) bounds) Int)))) (tail-call/right/std/vector/deque/map half (- len 1)) result))) (let std/vector/deque/balance? (lambda q (= (+ (std/vector/deque/offset-right q) (std/vector/deque/offset-left q)) 0))) (let std/convert/vector->deque (lambda initial (do (let q (std/vector/deque/new)) (let half (/ (length initial) 2)) (letrec tail-call/left/from/vector->deque (lambda index (do (std/vector/deque/add-to-left! q (get initial index)) (if (> index 0) (tail-call/left/from/vector->deque (- index 1)) Int)))) (tail-call/left/from/vector->deque (- half 1)) (letrec tail-call/right/from/vector->deque (lambda index bounds (do (std/vector/deque/add-to-right! q (get initial index)) (if (< index bounds) (tail-call/right/from/vector->deque (+ index 1) bounds) Int)))) (tail-call/right/from/vector->deque half (- (length initial) 1)) q))) (let std/convert/deque->vector (lambda q (if (std/vector/deque/empty? q) (vector (get q 0 0)) (do (let out (vector)) (letrec tail-call/from/deque->vector (lambda index bounds (do (set! out (length out) (std/vector/deque/get q index)) (if (< index bounds) (tail-call/from/deque->vector (+ index 1) bounds) Int)))) (tail-call/from/deque->vector 0 (- (std/vector/deque/length q) 1)) out)))) (let std/vector/deque/balance! (lambda q (if (std/vector/deque/balance? q) q (do (let initial (std/convert/deque->vector q)) (std/vector/deque/empty! q) (let half (/ (length initial) 2)) (letrec tail-call/left/std/vector/deque/balance! (lambda index (do (std/vector/deque/add-to-left! q (get initial index)) (if (> index 0) (tail-call/left/std/vector/deque/balance! (- index 1)) Int)))) (letrec tail-call/right/std/vector/deque/balance! (lambda index bounds (do (std/vector/deque/add-to-right! q (get initial index)) (if (< index bounds) (tail-call/right/std/vector/deque/balance! (+ index 1) bounds) Int)))) (tail-call/right/std/vector/deque/balance! half (- (length initial) 1)) (if (> (length initial) 1) (tail-call/left/std/vector/deque/balance! (- half 1)) Int) q)))) (let std/vector/deque/append! (lambda q item (do (std/vector/deque/add-to-right! q item) q))) (let std/vector/deque/prepend! (lambda q item (do (std/vector/deque/add-to-left! q item) q))) (let std/vector/deque/head! (lambda q (do (if (= (std/vector/deque/offset-right q) 0) (std/vector/deque/balance! q) q) (std/vector/deque/remove-from-right! q) q))) (let std/vector/deque/tail! (lambda q (do (if (= (std/vector/deque/offset-left q) 0) (std/vector/deque/balance! q) q) (std/vector/deque/remove-from-left! q) q))) (let std/vector/deque/first (lambda q (std/vector/deque/get q 0))) (let std/vector/deque/last (lambda q (std/vector/deque/get q (- (std/vector/deque/length q) 1)))) (let std/vector/deque/pop-right! (lambda q (do (let last (std/vector/deque/last q)) (std/vector/deque/head! q) last))) (let std/vector/deque/pop-left! (lambda q (do (let f (std/vector/deque/first q)) (std/vector/deque/tail! q) f))) (let std/vector/deque/rotate-left! (lambda q n (do (let N (mod n (std/vector/deque/length q))) (letrec tail-call/std/vector/deque/rotate-left! (lambda index bounds (do (if (= (std/vector/deque/offset-left q) 0) (std/vector/deque/balance! q) q) (std/vector/deque/add-to-right! q (std/vector/deque/first q)) (std/vector/deque/remove-from-left! q) (if (< index bounds) (tail-call/std/vector/deque/rotate-left! (+ index 1) bounds) Int)))) (tail-call/std/vector/deque/rotate-left! 0 N) q))) (let std/vector/deque/rotate-right! (lambda q n (do (let N (mod n (std/vector/deque/length q))) (letrec tail-call/std/vector/deque/rotate-left! (lambda index bounds (do (if (= (std/vector/deque/offset-right q) 0) (std/vector/deque/balance! q) q) (std/vector/deque/add-to-left! q (std/vector/deque/last q)) (std/vector/deque/remove-from-right! q) (if (< index bounds) (tail-call/std/vector/deque/rotate-left! (+ index 1) bounds) Int)))) (tail-call/std/vector/deque/rotate-left! 0 N) q))) (let std/vector/deque/slice (lambda entity s e (do (let len (std/vector/deque/length entity)) (let start (if (< s 0) (std/int/max (+ len s) 0) (std/int/min s len))) (let end (if (< e 0) (std/int/max (+ len e) 0) (std/int/min e len))) (let scl (std/vector/deque/new)) (let slice-len (std/int/max (- end start) 0)) (let half (/ slice-len 2)) (letrec tail-call/left/std/vector/deque/slice (lambda index (do (std/vector/deque/add-to-left! scl (std/vector/deque/get entity (+ start index))) (if (> index 0) (tail-call/left/std/vector/deque/slice (- index 1)) Int)))) (tail-call/left/std/vector/deque/slice (- half 1)) (letrec tail-call/right/std/vector/deque/slice (lambda index bounds (do (std/vector/deque/add-to-right! scl (std/vector/deque/get entity (+ start index))) (if (< index bounds) (tail-call/right/std/vector/deque/slice (+ index 1) bounds) Int)))) (tail-call/right/std/vector/deque/slice half (- slice-len 1)) scl))) (let std/vector/queue/new std/vector/deque/new) (let std/vector/stack/new std/vector/deque/new) (let std/vector/queue/empty? std/vector/deque/empty?) (let std/vector/queue/not-empty? (lambda q (not (std/vector/deque/empty? q)))) (let std/vector/queue/empty! std/vector/deque/empty!) (let std/vector/queue/enqueue! (lambda queue item (std/vector/deque/append! queue item))) (let std/vector/queue/dequeue! (lambda queue (std/vector/deque/tail! queue))) (let std/vector/queue/peek (lambda queue (std/vector/deque/first queue))) (let std/vector/stack/empty? std/vector/deque/empty?) (let std/vector/stack/not-empty? (lambda q (not (std/vector/deque/empty? q)))) (let std/vector/stack/empty! std/vector/deque/empty!) (let std/vector/stack/push! (lambda stack item (std/vector/deque/append! stack item))) (let std/vector/stack/pop! (lambda stack (std/vector/deque/head! stack))) (let std/vector/stack/peek (lambda stack (std/vector/deque/last stack))) (let std/vector/3d/for (lambda matrix fn (do (let width (length (std/vector/first matrix))) (let height (length matrix)) (loop 0 height (lambda y (loop 0 width (lambda x (fn (get matrix y x)))))) matrix))) (let std/vector/3d/for/i (lambda matrix fn (do (let width (length (std/vector/first matrix))) (let height (length matrix)) (loop 0 height (lambda y (loop 0 width (lambda x (fn (get matrix y x) y x))))) matrix))) (let std/vector/3d/points (lambda matrix fn? (do (let coords (vector)) (std/vector/3d/for/i matrix (lambda cell y x (if (fn? cell) (do (std/vector/push! coords (vector y x)) nil)))) coords))) (let std/vector/concat/with (lambda xs ch (std/vector/reduce/i xs (lambda a b i (if (and (> i 0) (< i (length xs))) (std/vector/cons (std/vector/cons a (vector ch)) b) (std/vector/cons a b))) (vector)))) (let std/vector/char/lines (lambda xs (std/convert/string->vector xs std/char/new-line))) (let std/vector/char/words (lambda xs (std/convert/string->vector xs std/char/space))) (let std/vector/char/commas (lambda xs (std/convert/string->vector xs std/char/comma))) (let std/vector/int/pair/sub (lambda xs (- (get xs 0) (get xs 1)))) (let std/vector/int/pair/add (lambda xs (+ (get xs 0) (get xs 1)))) (let std/vector/int/pair/mult (lambda xs (* (get xs 0) (get xs 1)))) (let std/vector/int/pair/div (lambda xs (/ (get xs 0) (get xs 1)))) (let std/vector/sort/asc! (lambda xs (std/vector/sort! xs <))) (let std/vector/sort/desc! (lambda xs (std/vector/sort! xs >))) (let std/vector/equal? (lambda a b fn? (do (if (< (length a) (length b)) false (if (> (length a) (length b)) false (do (mut i 0) (mut result true) (let len (length a)) (while (< i len) (do (let da (get a i)) (let db (get b i)) (if (not (fn? da db)) (do (alter! result false) (alter! i len))) (alter! i (+ i 1)))) (if result true false))))))) (let std/vector/char/lexicographic (lambda a b eq? gt? (do (mut i 0) (let len-a (length a)) (let len-b (length b)) (let len (if (< len-a len-b) len-a len-b)) (mut out 0) (while (and (< i len) (= out 0)) (do (let da (get a i)) (let db (get b i)) (if (not (eq? da db)) (alter! out (if (gt? da db) 1 -1)) nil) (alter! i (+ i 1)))) (if (= out 0) (if (= len-a len-b) 0 (if (> len-a len-b) 1 -1)) out)))) (let std/vector/compare (lambda a b step done? initial (do (mut state initial) (mut i 0) (let len-a (length a)) (let len-b (length b)) (let len (if (< len-a len-b) len-a len-b)) (while (and (< i len) (not (done? state))) (do (alter! state (step state (get a i) (get b i) i)) (alter! i (+ i 1)))) (tuple state (tuple len-a len-b))))) (let std/vector/int/equal? (lambda a b (do (if (< (length a) (length b)) false (if (> (length a) (length b)) false (do (mut i 0) (mut result true) (let len (length a)) (while (< i len) (do (let da (get a i)) (let db (get b i)) (if (not (= da db)) (do (alter! result false) (alter! i len))) (alter! i (+ i 1)))) (if result true false))))))) (let std/vector/dec/equal? (lambda a b (do (if (< (length a) (length b)) false (if (> (length a) (length b)) false (do (mut i 0) (mut result true) (let len (length a)) (while (< i len) (do (let da (get a i)) (let db (get b i)) (if (not (=. da db)) (do (alter! result false) (alter! i len))) (alter! i (+ i 1)))) (if result true false))))))) (let std/vector/bool/equal? (lambda a b (do (if (< (length a) (length b)) false (if (> (length a) (length b)) false (do (mut i 0) (mut result true) (let len (length a)) (while (< i len) (do (let da (get a i)) (let db (get b i)) (if (not (=? da db)) (do (alter! result false) (alter! i len))) (alter! i (+ i 1)))) (if result true false))))))) (let std/convert/integer->bits (lambda num (if (= num 0) (vector 0) (do (&mut n num) (letrec tail-call/while (lambda out (if (> (&get n) 0) (do (std/vector/push! out (mod (get n) 2)) (&alter! n (/ (&get n) 2)) (tail-call/while out)) out))) (std/vector/reverse (tail-call/while (vector))))))) (let std/vector/subset (lambda xs (if (std/vector/empty? xs) (vector xs) (do (let n (length xs)) (let out (vector)) (loop 0 (std/int/expt 2 n) (lambda i (std/vector/push! out (<| i (std/convert/integer->bits) (std/vector/reduce/i (lambda a x i (if (= x 1) (std/vector/append! a (get xs i)) a)) (vector)))))) out)))) (let std/vector/flat-map (lambda xs (std/vector/map (std/vector/flat-one xs)))) (let std/convert/bits->integer (lambda xs (do (letrec tail-call/bits->integer (lambda index out (if (= index (length xs)) out (tail-call/bits->integer (+ index 1) (+ out (* (std/vector/at xs index) (std/int/expt 2 (- (length xs) index 1)))))))) (tail-call/bits->integer 0 0)))) (let std/vector/copy (lambda xs (std/vector/map xs identity))) (let std/int/reduce (lambda n fn acc (do (letrec tail-call/fold-n (lambda i out (if (< i n) (tail-call/fold-n (+ i 1) (fn out i)) out))) (tail-call/fold-n 0 acc)))) (let std/vector/2d/fill (lambda n fn (do (letrec tail-call/std/vector/fill (lambda i xs (if (= i 0) xs (tail-call/std/vector/fill (- i 1) (std/vector/cons! xs (vector (fn i))))))) (tail-call/std/vector/fill n (vector))))) (let std/vector/3d/fill (lambda W H fn (cond (or (= W 0) (= H 0)) (vector) (and (= W 1) (= H 1)) (vector (vector (fn 0 0))) (do (let matrix (vector)) (loop 0 W (lambda i (do (std/vector/push! matrix (vector)) (loop 0 H (lambda j (std/vector/3d/set! matrix i j (fn i j))))))) matrix)))) (let std/vector/3d/product (lambda A B (do (let dimsA (std/vector/3d/dimensions A)) (let dimsB (std/vector/3d/dimensions B)) (let rowsA (get dimsA 0)) (let colsA (get dimsA 1)) (let rowsB (get dimsB 0)) (let colsB (get dimsB 1)) (if (= colsA rowsB) (std/vector/3d/fill rowsA colsB (lambda i j (std/int/reduce colsA (lambda sm k (+ sm (* (get A i k) (get B k j)))) 0))) (vector))))) (let std/vector/3d/dot-product (lambda a b (do (let lenA (length a)) (let lenB (length b)) (if (= lenA lenB) (std/int/reduce lenA (lambda sm i (+ sm (* (get a i) (get b i)))) 0) Int)))) (let std/vector/3d/rotate (lambda matrix (if (std/vector/empty? matrix) matrix (do (let H (length matrix)) (let W (length (get matrix 0))) (let out (vector)) (mut i 0) (while (< i W) (do (mut j 0) (std/vector/push! out (vector)) (while (< j H) (do (std/vector/push! (std/vector/at out -1) (get matrix j i)) (alter! j (+ j 1)))) (alter! i (+ i 1)))) out)))) (let std/vector/2d/interleave (lambda xs ys (do (let out (vector)) (loop 0 (std/int/min (length xs) (length ys)) (lambda i (do (std/vector/push! out (get xs i)) (std/vector/push! out (get ys i))))) out))) (let std/vector/intersperse (lambda xs x (if (std/vector/empty? xs) (vector) (do (let out (vector)) (loop 0 (- (length xs) 1) (lambda i (do (std/vector/push! out (get xs i)) (std/vector/push! out x)))) (std/vector/push! out (get xs (- (length xs) 1))) out)))) (let std/vector/int/sequence (lambda xs (std/vector/int/range 0 (- (length xs) 1)))) (let std/int/shoelace (lambda px (do (let len (length px)) (/ (<| (std/vector/int/sequence px) (std/vector/reduce (lambda ab i (do (let a (get ab 0)) (let b (get ab 1)) (let left (get px i)) (let right (get px (mod (+ i 1) len))) (let y1 (get left 0)) (let x1 (get left 1)) (let y2 (get right 0)) (let x2 (get right 1)) (vector (+ a (* y1 x2)) (+ b (* y2 x1))))) (vector 0 0)) (std/vector/int/pair/sub) (std/int/abs)) 2)))) (let std/int/collinear? (lambda px (= (std/int/shoelace px) 0))) (let std/vector/int/big/range (lambda start end (do (let out (vector (std/int/big/new (std/convert/integer->string start)))) (loop (+ start 1) (+ end 1) (lambda i (set! out (length out) (std/int/big/new (std/convert/integer->string i))))) out))) (let std/int/big/add (lambda a1 b1 (do (let a (std/vector/reverse a1)) (let b (std/vector/reverse b1)) (let max-length (std/int/max (length a) (length b))) (let result (vector)) (mut carry 0) (loop/range/exclusive i 0 max-length (let digit-A (if (< i (length a)) (get a i) 0)) (let digit-B (if (< i (length b)) (get b i) 0)) (let sm (+ digit-A digit-B carry)) (std/vector/push! result (mod sm 10)) (alter! carry (/ sm 10))) (while (> carry 0) (do (std/vector/push! result (mod carry 10)) (alter! carry (/ carry 10)))) (std/vector/reverse result)))) (let std/int/big/sub (lambda a1 b1 (do (let a (std/vector/reverse a1)) (let b (std/vector/reverse b1)) (let result (vector)) (mut borrow 0) (mut j 0) (let max-length (std/int/max (length a) (length b))) (while (< j max-length) (do (let digit-a (if (< j (length a)) (get a j) 0)) (let digit-b (if (< j (length b)) (get b j) 0)) (let diff (- (- digit-a digit-b) borrow)) (if (< diff 0) (do (push! result (+ diff 10)) (alter! borrow 1)) (do (push! result diff) (alter! borrow 0))) (alter! j (+ j 1)))) (mut k (- (length result) 1)) (while (and (> k 0) (= (get result k) 0)) (do (pop! result) (alter! k (- k 1)))) (std/vector/reverse result)))) (let std/int/big/mul (lambda a1 b1 (do (let a (std/vector/reverse a1)) (let b (std/vector/reverse b1)) (let result (vector)) (loop/range/exclusive _ 0 (+ (length a) (length b)) (std/vector/push! result 0)) (loop/range/exclusive i 0 (length a) (mut carry 0) (let digit-a (get a i)) (loop/range/exclusive j 0 (length b) (let digit-B (get b j)) (let idx (+ i j)) (let prod (+ (* digit-a digit-B) (get result idx) carry)) (set! result idx (mod prod 10)) (alter! carry (/ prod 10))) (mut k (+ i (length b))) (while (> carry 0) (do (if (not (< k (length result))) (do (std/vector/push! result 0) nil) nil) (let sm (+ (get result k) carry)) (set! result k (mod sm 10)) (alter! carry (/ sm 10)) (alter! k (+ k 1))))) (mut ii (- (length result) 1)) (while (and (> ii 0) (= (get result ii) 0) (> (length result) 1)) (do (pop! result) (alter! ii (- ii 1)))) (std/vector/reverse result)))) (let std/vector/int/remove-leading-zeroes (lambda digits (do (mut i 0) (while (and (< i (length digits)) (std/int/zero? (get digits i))) (do (++ i))) (if (= i (length digits)) (vector 0) (std/vector/slice digits i (length digits)))))) (let std/int/big/less-or-equal? (lambda a b (do (if (< (length a) (length b)) true (if (> (length a) (length b)) false (do (mut i 0) (mut result true) (while (< i (length a)) (do (let da (get a i)) (let db (get b i)) (if (< da db) (do (alter! result true) (alter! i (length a)))) (if (> da db) (do (alter! result false) (alter! i (length a)))) (alter! i (+ i 1)))) result)))))) (let std/int/big/greater-or-equal? (lambda a b (do (if (> (length a) (length b)) true (if (< (length a) (length b)) false (do (mut i 0) (mut result true) (while (< i (length a)) (do (let da (get a i)) (let db (get b i)) (if (> da db) (do (alter! result true) (alter! i (length a)))) (if (< da db) (do (alter! result false) (alter! i (length a)))) (alter! i (+ i 1)))) result)))))) (let std/int/big/less-than? (lambda a b (do (if (< (length a) (length b)) true (if (> (length a) (length b)) false (do (mut i 0) (mut found-less false) (while (< i (length a)) (do (let da (get a i)) (let db (get b i)) (if (< da db) (do (alter! found-less true) (alter! i (length a)))) (if (> da db) (do (alter! i (length a)))) (alter! i (+ i 1)))) found-less)))))) (let std/int/big/greater-than? (lambda a b (do (if (> (length a) (length b)) true (if (< (length a) (length b)) false (do (mut i 0) (mut found-greater false) (while (< i (length a)) (do (let da (get a i)) (let db (get b i)) (if (> da db) (do (alter! found-greater true) (alter! i (length a)))) (if (< da db) (do (alter! i (length a)))) (alter! i (+ i 1)))) found-greater)))))) (let std/int/big/equal? std/vector/int/equal?) (let std/int/big/div (lambda dividend divisor (do (let result (vector)) (&mut current (vector)) (let len (length dividend)) (mut i 0) (while (< i len) (do (let digit (get dividend i)) (let cur0 (&get current)) (let cur1 (cons cur0 (vector digit))) (let cur2 (std/vector/int/remove-leading-zeroes cur1)) (&alter! current cur2) (mut low 0) (mut high 9) (mut q 0) (while (<= low high) (do (let mid (/ (+ low high) 2)) (let prod (std/int/big/mul divisor (vector mid))) (let cur (&get current)) (if (std/int/big/less-or-equal? prod cur) (do (alter! q mid) (alter! low (+ mid 1))) (alter! high (- mid 1))))) (std/vector/push! result q) (let prod2 (std/int/big/mul divisor (vector q))) (let cur3 (&get current)) (let cur4 (std/int/big/sub cur3 prod2)) (&alter! current cur4) (alter! i (+ i 1)))) (let out (std/vector/int/remove-leading-zeroes result)) (if (std/vector/empty? out) (vector 0) out)))) (let std/int/big/mod (lambda a b (do (let q (std/int/big/div a b)) (let p (std/int/big/mul b q)) (let r (std/int/big/sub a p)) r))) (let std/int/big/square (lambda x (std/int/big/mul x x))) (let std/int/big/floor/div (lambda a b (std/int/big/div a b))) (let std/int/big/ceil/div (lambda a b (std/int/big/div (std/int/big/sub (std/int/big/add a b) (vector 1)) b))) (let std/vector/int/big/sum (lambda xs (std/vector/reduce xs (lambda a b (std/int/big/add a b)) (vector 0)))) (let std/vector/int/big/product (lambda xs (std/vector/reduce xs (lambda a b (std/int/big/mul a b)) (vector 1)))) (let std/int/big/new (lambda str (std/convert/chars->digits str))) (let std/int/pow/big (lambda n pow (do (let digits (vector n)) (mut p 1) (mut carry 0) (while (< p pow) (do (alter! carry 0) (loop 0 (length digits) (lambda exp (do (let prod (+ (* (get digits exp) n) carry)) (let new-carry (/ prod 10)) (set! digits exp (mod prod 10)) (alter! carry new-carry)))) (while (> carry 0) (do (std/vector/push! digits (mod carry 10)) (alter! carry (/ carry 10)))) (alter! p (+ p 1)))) (std/vector/reverse digits)))) (let std/int/big/pow (lambda a b (if (= b 0) (vector 1) (do (&mut out a) (mut i 0) (while (< i (- b 1)) (do (&alter! out (std/int/big/mul (&get out) a)) (alter! i (+ i 1)))) (&get out))))) (let std/int/big/expt (lambda a b (if (and (= (length b) 1) (= (get b 0) 0)) (vector 1) (do (&mut out a) (&mut exp (std/int/big/sub b (vector 1))) (while (not (and (= (length (&get exp)) 1) (= (get (&get exp) 0) 0))) (do (&alter! out (std/int/big/mul (&get out) a)) (&alter! exp (std/int/big/sub (&get exp) (vector 1))))) (&get out))))) (let std/int/big/signed/zero (tuple false (vector 0))) (let std/int/big/signed/one (tuple false (vector 1))) (let std/int/big/signed/negative? fst) (let std/int/big/signed/digits snd) (let std/int/big/signed/abs snd) (let std/int/big/signed/zero? (lambda n (std/int/big/equal? (snd n) (vector 0)))) (let std/int/big/signed/normalize (lambda (tuple neg digits) (do (let mag (std/vector/int/remove-leading-zeroes digits)) (if (or (std/vector/empty? mag) (std/int/big/equal? mag (vector 0))) std/int/big/signed/zero (tuple neg mag))))) (let std/int/big/signed/new (lambda str (if (and (> (length str) 0) (=# (get str 0) (char 45))) (std/int/big/signed/normalize (tuple true (std/int/big/new (std/vector/slice str 1 (length str))))) (std/int/big/signed/normalize (tuple false (std/int/big/new str)))))) (let std/int/big/signed/negate (lambda (tuple neg digits) (if (std/int/big/equal? digits (vector 0)) std/int/big/signed/zero (tuple (not neg) digits)))) (let std/int/big/signed/equal? (lambda a b (and (=? (fst a) (fst b)) (std/int/big/equal? (snd a) (snd b))))) (let std/int/big/signed/lt? (lambda a b (if (fst a) (if (fst b) (std/int/big/greater-than? (snd a) (snd b)) true) (if (fst b) false (std/int/big/less-than? (snd a) (snd b)))))) (let std/int/big/signed/lte? (lambda a b (or (std/int/big/signed/equal? a b) (std/int/big/signed/lt? a b)))) (let std/int/big/signed/gt? (lambda a b (not (std/int/big/signed/lte? a b)))) (let std/int/big/signed/gte? (lambda a b (not (std/int/big/signed/lt? a b)))) (let std/int/big/signed/add (lambda a b (if (=? (fst a) (fst b)) (std/int/big/signed/normalize (tuple (fst a) (std/int/big/add (snd a) (snd b)))) (if (std/int/big/greater-than? (snd a) (snd b)) (std/int/big/signed/normalize (tuple (fst a) (std/int/big/sub (snd a) (snd b)))) (if (std/int/big/less-than? (snd a) (snd b)) (std/int/big/signed/normalize (tuple (fst b) (std/int/big/sub (snd b) (snd a)))) std/int/big/signed/zero))))) (let std/int/big/signed/sub (lambda a b (std/int/big/signed/add a (std/int/big/signed/negate b)))) (let std/int/big/signed/mul (lambda a b (std/int/big/signed/normalize (tuple (not (=? (fst a) (fst b))) (std/int/big/mul (snd a) (snd b)))))) (let std/int/big/signed/square (lambda x (std/int/big/signed/mul x x))) (let std/int/big/signed/pow (lambda a b (if (= b 0) std/int/big/signed/one (std/int/big/signed/normalize (tuple (and (fst a) (= (mod b 2) 1)) (std/int/big/pow (snd a) b)))))) (let std/int/big/signed/expt (lambda a b (if (std/int/big/equal? b (vector 0)) std/int/big/signed/one (std/int/big/signed/normalize (tuple (and (fst a) (std/int/big/equal? (std/int/big/mod b (vector 2)) (vector 1))) (std/int/big/expt (snd a) b)))))) (let std/int/big/signed/div (lambda a b (do (let q (std/int/big/div (snd a) (snd b))) (std/int/big/signed/normalize (tuple (not (=? (fst a) (fst b))) q))))) (let std/int/big/signed/mod (lambda a b (std/int/big/signed/sub a (std/int/big/signed/mul b (std/int/big/signed/div a b))))) (let std/int/big/signed/floor/div (lambda a b (do (let q (std/int/big/signed/div a b)) (let r (std/int/big/signed/mod a b)) (if (and (not (std/int/big/signed/zero? r)) (not (=? (fst a) (fst b)))) (std/int/big/signed/sub q std/int/big/signed/one) q)))) (let std/int/big/signed/ceil/div (lambda a b (do (let q (std/int/big/signed/div a b)) (let r (std/int/big/signed/mod a b)) (if (and (not (std/int/big/signed/zero? r)) (=? (fst a) (fst b))) (std/int/big/signed/add q std/int/big/signed/one) q)))) (let std/int/big/signed/to-string (lambda n (if (fst n) (cons (string 45) (std/convert/digits->chars (snd n))) (std/convert/digits->chars (snd n))))) (let std/vector/int/big/signed/range (lambda start end (if (std/int/big/signed/gt? start end) (vector) (do (let out (vector start)) (&mut current start) (while (std/int/big/signed/lt? (&get current) end) (do (&alter! current (std/int/big/signed/add (&get current) std/int/big/signed/one)) (set! out (length out) (&get current)))) out)))) (let std/vector/int/big/signed/sum (lambda xs (std/vector/reduce xs (lambda a b (std/int/big/signed/add a b)) std/int/big/signed/zero))) (let std/vector/int/big/signed/product (lambda xs (std/vector/reduce xs (lambda a b (std/int/big/signed/mul a b)) std/int/big/signed/one))) (let std/convert/integer->digits-base (lambda num base (if (= num 0) (vector 0) (do (integer n num) (letrec tail-call/while (lambda out (if (> (get n) 0) (do (std/vector/push! out (mod (get n) base)) (set n (/ (get n) base)) (tail-call/while out)) out))) (let digits (tail-call/while (vector))) (std/vector/reverse digits))))) (let std/convert/integer->digits (lambda num (std/convert/integer->digits-base num 10))) (let std/vector/adjacent-difference (lambda xsi fn (do (let len (length xsi)) (let xs (std/vector/copy xsi)) (mut i 1) (while (< i len) (do (std/vector/update! xs i (fn (get xs (- i 1)) (get xs i))) (alter! i (+ i 1)))) xs))) (let std/vector/adjacent-difference! (lambda xs fn (do (let len (length xs)) (unless (= len 1) (do (mut i 1) (while (< i len) (do (std/vector/update! xs i (fn (get xs (- i 1)) (get xs i))) (alter! i (+ i 1)))) nil))))) (let std/convert/vector/3d->string (lambda xs a b (std/convert/vector->string (std/vector/map xs (lambda x (std/convert/vector->string x b))) a))) (let std/vector/cycle (lambda n xs (do (let out (vector)) (let len (length xs)) (loop 0 n (lambda i (std/vector/push! out (get xs (mod i len))))) out))) (let std/vector/replicate (lambda n x (do (let out (vector)) (loop 0 n (lambda _ (std/vector/push! out x))) out))) (let std/vector/int/extreme (lambda xs (tuple (std/vector/int/minimum xs) (std/vector/int/maximum xs)))) (let std/tuple/map (lambda (tuple a b) fn (fn a b))) (let std/tuple/map/fst (lambda (tuple a _) fn (fn a))) (let std/tuple/map/snd (lambda (tuple _ b) fn (fn b))) (let std/tuple/swap (lambda (tuple a b) (tuple b a))) (let get* (lambda xs i some none (if (std/vector/in-bounds? xs i) (do (some (get xs i)) nil) (do (none) nil)))) (let get* (lambda xs i some none (if (std/vector/in-bounds? xs i) (do (some (get xs i)) nil) (do (none) nil)))) (let std/vector/2d/get* get*) (let std/vector/3d/get* (lambda xs i j some none (if (std/vector/3d/in-bounds? xs i j) (do (some (get xs i j)) nil) (do (none) nil)))) (let std/vector/enumerate (lambda xs (std/vector/tuple/zip (tuple (std/vector/int/range 0 (- (length xs) 1)) xs)))) (let std/int/factorial (lambda n (do (letrec fact (lambda n total (if (= n 0) total (fact (- n 1) (* total n))))) (fact n 1)))) (let std/dec/factorial (lambda n (do (letrec fact (lambda n total (if (=. n 0.0) total (fact (-. n 1.0) (*. total n))))) (fact n 1.0)))) (let std/vector/permutations (lambda arr (do (letrec permute (lambda arr (if (<= (length arr) 1) (vector arr) (do (let out (vector)) (&mut i 0) (while (< (&get i) (length arr)) (do (let rest (std/vector/filter/i arr (lambda y j (not (= j (&get i)))))) (let perms (permute rest)) (let x (get arr (&get i))) (&mut j 0) (while (< (&get j) (length perms)) (do (set! out (length out) (std/vector/cons! (vector x) (get perms (&get j)))) (&alter! j (+ (&get j) 1)))) (&alter! i (+ (&get i) 1)))) out)))) (permute arr)))) (let std/vector/combinations (lambda xs (do (let out (vector)) (letrec combinations (lambda arr size start temp (if (= (length temp) size) (set! out (length out) (std/vector/copy temp)) (loop start (length arr) (lambda i (do (set! temp (length temp) (get arr i)) (combinations arr size (+ i 1) temp) (pop! temp))))))) (loop 1 (+ 1 (length xs)) (lambda i (combinations xs i 0 (vector)))) out))) (let std/vector/combinations/n (lambda xs n (do (let out (vector)) (letrec combinations (lambda arr size start temp (if (= (length temp) size) (set! out (length out) (std/vector/copy temp)) (loop start (length arr) (lambda i (do (set! temp (length temp) (get arr i)) (combinations arr size (+ i 1) temp) (pop! temp))))))) (combinations xs n 0 (vector)) out))) (let std/int/div/option (lambda a b (if (= b 0) (tuple false 0) (tuple true (/ a b))))) (let std/int/expt/option (lambda a b (if (< a 0) (tuple false 0) (tuple true (std/int/expt a b))))) (let std/int/mod/option (lambda a b (if (= b 0) (tuple false 0) (tuple true (mod a b))))) (let std/int/sqrt/option (lambda n (if (< n 0) (tuple false 0) (tuple true (std/int/sqrt n))))) (let std/dec/div/option (lambda a b (if (=. b 0.0) (tuple false 0.0) (tuple true (/. a b))))) (let std/dec/expt/option (lambda a b (if (<. a 0.0) (tuple false 0.0) (tuple true (std/dec/expt a b))))) (let std/dec/log/option (lambda x (if (<=. x 0.0) (tuple false 0.0) (tuple true (std/dec/log x))))) (let std/dec/mod/option (lambda a b (if (=. b 0.0) (tuple false 0.0) (tuple true (mod. a b))))) (let std/dec/sqrt/option (lambda n (if (<. n 0.0) (tuple false 0.0) (tuple true (std/dec/sqrt n))))) (let std/true/option (lambda x (tuple true x))) (let std/false/option (lambda x (tuple false x))) (let std/vector/option/resolve (lambda xs fn df (if (std/vector/every? xs fst) (tuple true (fn (std/vector/map xs snd))) (tuple false df)))) (let std/fn/exec (lambda xs fn (fn xs))) (let std/convert/vector->tuple (lambda xs fn1 fn2 (tuple (fn1 xs) (fn2 xs)))) (let std/tuple/int/add (lambda (tuple a b) (+ a b))) (let std/tuple/int/sub (lambda (tuple a b) (- a b))) (let std/tuple/int/mul (lambda (tuple a b) (* a b))) (let std/tuple/int/div (lambda (tuple a b) (* a b))) (let loop/repeat (lambda n fn (loop 0 n (lambda _ (fn))))) (let loop/some-range? (lambda start end predicate? (do (letrec tail-call/loop/some-range? (lambda i out (if (< i end) (if (predicate? i) true (tail-call/loop/some-range? (+ i 1) out)) out))) (tail-call/loop/some-range? start false)))) (let loop/some-n? (lambda n predicate? (loop/some-range? 0 n predicate?))) (let push! (lambda xs x (set! xs (length xs) x))) (let pull! std/vector/pop-and-get!) (let swap! std/vector/swap!) (let scan! (lambda xs fn (std/vector/adjacent-difference! xs fn))) (let empty! (lambda xs (do (std/vector/empty! xs) nil))) (let reverse! std/vector/reverse!) (let sort! std/vector/sort!) (let emod std/int/euclidean-mod) (let mul std/int/mul) (let div std/int/div) (let add std/int/add) (let sub std/int/sub) (let std/int/hash (lambda table key (do (let cap (length table)) (if (= cap 0) 0 (do (mut i 0) (mut hash 0) (let len (length key)) (while (< i len) (do (alter! hash (std/int/euclidean-mod (+ (* hash 131) (as (get key i) Int)) cap)) (alter! i (+ i 1)))) hash))))) (let std/vector/hash/set (lambda capacity (std/vector/buckets (std/int/max 4 capacity)))) (let std/vector/hash/set/new std/vector/hash/set) (let std/vector/hash/set/max-capacity (lambda a b (std/vector/hash/set (std/int/max (length a) (length b))))) (let std/vector/hash/set/min-capacity (lambda a b (std/vector/hash/set (std/int/min (length a) (length b))))) (let std/vector/hash/set/key-equal? (lambda a b (do (let len (length a)) (if (not (= len (length b))) false (do (mut i 0) (mut matches true) (while (and matches (< i len)) (do (if (not (=# (get a i) (get b i))) (alter! matches false) nil) (alter! i (+ i 1)))) matches))))) (let std/vector/hash/set/find-index (lambda bucket key (do (mut i 0) (mut found -1) (let len (length bucket)) (while (and (= found -1) (< i len)) (do (if (std/vector/hash/set/key-equal? (get bucket i) key) (alter! found i) nil) (alter! i (+ i 1)))) found))) (let std/vector/hash/set/count (lambda table (do (mut total 0) (let len (length table)) (mut i 0) (while (< i (length table)) (do (alter! total (+ total (length (get table i)))) (alter! i (+ i 1)))) total))) (let std/vector/hash/set/for-each (lambda table fn (do (mut i 0) (let table-len (length table)) (while (< i table-len) (do (let bucket (get table i)) (mut j 0) (let bucket-len (length bucket)) (while (< j bucket-len) (do (fn (get bucket j)) (alter! j (+ j 1)))) (alter! i (+ i 1))))))) (let std/vector/hash/set/add!/raw (lambda table key (do (let idx (std/int/hash table key)) (let bucket (get table idx)) (set! bucket (length bucket) key) table))) (let std/vector/hash/set/resize! (lambda table new-capacity (do (let target (std/int/max 4 new-capacity)) (if (= target (length table)) table (do (let entries (vector)) (std/vector/hash/set/for-each table (lambda key (set! entries (length entries) key))) (std/vector/empty! table) (mut i 0) (while (< i target) (do (set! table (length table) (vector)) (alter! i (+ i 1)))) (mut j 0) (let entries-len (length entries)) (while (< j entries-len) (do (std/vector/hash/set/add!/raw table (get entries j)) (alter! j (+ j 1)))) table))))) (let std/vector/hash/set/compact! (lambda table (do (let used (std/vector/hash/set/count table)) (let target (std/int/max 32 (* used 2))) (std/vector/hash/set/resize! table target)))) (let std/vector/hash/set/has? (lambda table key (if (= (length table) 0) false (do (let idx (std/int/hash table key)) (let bucket (get table idx)) (>= (std/vector/hash/set/find-index bucket key) 0))))) (let std/vector/hash/set/add! (lambda table key (do (if (= (length table) 0) (do (std/vector/hash/set/resize! table 32) nil) nil) (let idx (std/int/hash table key)) (let bucket (get table idx)) (if (= (std/vector/hash/set/find-index bucket key) -1) (do (set! bucket (length bucket) key) (if (> (length bucket) 8) (do (std/vector/hash/set/resize! table (* (length table) 2)) nil) nil)) nil) table))) (let std/vector/hash/set/remove! (lambda table key (do (if (= (length table) 0) table (do (let idx (std/int/hash table key)) (let bucket (get table idx)) (let index (std/vector/hash/set/find-index bucket key)) (if (>= index 0) (do (set! bucket index (get bucket (- (length bucket) 1))) (pop! bucket)) nil) (if (and (> (length table) 32) (= (length bucket) 0)) (do (let used (std/vector/hash/set/count table)) (if (< (* used 4) (length table)) (do (std/vector/hash/set/resize! table (std/int/max 32 (/ (length table) 2))) nil) nil)) nil) table))))) (let std/convert/vector->set/dynamic (lambda xs (do (let out (std/vector/hash/set (std/int/max 32 (length xs)))) (mut i 0) (let len (length xs)) (while (< i len) (do (std/vector/hash/set/add! out (get xs i)) (alter! i (+ i 1)))) out))) (let std/vector/hash/set/intersection (lambda a b (do (let out (std/vector/hash/set/max-capacity a b)) (let a-count (std/vector/hash/set/count a)) (let b-count (std/vector/hash/set/count b)) (let src (if (< a-count b-count) a b)) (let trg (if (< a-count b-count) b a)) (std/vector/hash/set/for-each src (lambda key (if (and (std/vector/not-empty? key) (std/vector/hash/set/has? trg key)) (do (std/vector/hash/set/add! out key) nil) nil))) out))) (let std/vector/hash/set/difference (lambda a b (do (let out (std/vector/hash/set/max-capacity a b)) (std/vector/hash/set/for-each a (lambda key (if (and (std/vector/not-empty? key) (not (std/vector/hash/set/has? b key))) (do (std/vector/hash/set/add! out key) nil) nil))) out))) (let std/vector/hash/set/xor (lambda a b (do (let out (std/vector/hash/set/max-capacity a b)) (std/vector/hash/set/for-each a (lambda key (if (and (std/vector/not-empty? key) (not (std/vector/hash/set/has? b key))) (do (std/vector/hash/set/add! out key) nil) nil))) (std/vector/hash/set/for-each b (lambda key (if (and (std/vector/not-empty? key) (not (std/vector/hash/set/has? a key))) (do (std/vector/hash/set/add! out key) nil) nil))) out))) (let std/vector/hash/set/union (lambda a b (do (let out (std/vector/hash/set/max-capacity a b)) (std/vector/hash/set/for-each a (lambda key (if (std/vector/not-empty? key) (do (std/vector/hash/set/add! out key) nil) nil))) (std/vector/hash/set/for-each b (lambda key (if (std/vector/not-empty? key) (do (std/vector/hash/set/add! out key) nil) nil))) out))) (let std/vector/hash/table (lambda capacity (std/vector/buckets (std/int/max 4 capacity)))) (let std/vector/hash/table/new std/vector/hash/table) (let std/vector/hash/table/max-capacity (lambda a b (std/vector/hash/table (std/int/max (length a) (length b))))) (let std/vector/hash/table/find-index (lambda bucket key (do (mut i 0) (mut found -1) (let len (length bucket)) (while (and (= found -1) (< i len)) (do (if (std/vector/hash/set/key-equal? (fst (get bucket i)) key) (alter! found i) nil) (alter! i (+ i 1)))) found))) (let std/vector/hash/table/for-each (lambda table fn (do (mut i 0) (let table-len (length table)) (while (< i table-len) (do (let bucket (get table i)) (mut j 0) (let bucket-len (length bucket)) (while (< j bucket-len) (do (fn (get bucket j)) (alter! j (+ j 1)))) (alter! i (+ i 1))))))) (let std/vector/hash/table/count-entries (lambda table (do (mut total 0) (mut i 0) (let len (length table)) (while (< i len) (do (alter! total (+ total (length (get table i)))) (alter! i (+ i 1)))) total))) (let std/vector/hash/table/set!/raw (lambda table key value (do (let idx (std/int/hash table key)) (let bucket (get table idx)) (set! bucket (length bucket) (tuple key value)) table))) (let std/vector/hash/table/resize! (lambda table new-capacity (do (let target (std/int/max 4 new-capacity)) (if (= target (length table)) table (do (let entries (vector)) (std/vector/hash/table/for-each table (lambda entry (set! entries (length entries) entry))) (std/vector/empty! table) (mut i 0) (while (< i target) (do (set! table (length table) (vector)) (alter! i (+ i 1)))) (mut j 0) (let entries-len (length entries)) (while (< j entries-len) (do (let entry (get entries j)) (std/vector/hash/table/set!/raw table (fst entry) (snd entry)) (alter! j (+ j 1)))) table))))) (let std/vector/hash/table/compact! (lambda table (do (let used (std/vector/hash/table/count-entries table)) (let target (std/int/max 32 (* used 2))) (std/vector/hash/table/resize! table target)))) (let std/vector/hash/table/has? (lambda table key (if (= (length table) 0) false (do (let idx (std/int/hash table key)) (let bucket (get table idx)) (>= (std/vector/hash/table/find-index bucket key) 0))))) (let std/vector/hash/table/set! (lambda table key value (do (if (= (length table) 0) (do (std/vector/hash/table/resize! table 32) nil) nil) (let idx (std/int/hash table key)) (let bucket (get table idx)) (let index (std/vector/hash/table/find-index bucket key)) (if (= index -1) (do (set! bucket (length bucket) (tuple key value)) (if (> (length bucket) 8) (do (std/vector/hash/table/resize! table (* (length table) 2)) nil) nil)) (set! bucket index (tuple key value))) table))) (let std/vector/hash/table/remove! (lambda table key (do (if (= (length table) 0) table (do (let idx (std/int/hash table key)) (let bucket (get table idx)) (let index (std/vector/hash/table/find-index bucket key)) (if (>= index 0) (do (set! bucket index (get bucket (- (length bucket) 1))) (pop! bucket)) nil) (if (and (> (length table) 32) (= (length bucket) 0)) (do (let used (std/vector/hash/table/count-entries table)) (if (< (* used 4) (length table)) (do (std/vector/hash/table/resize! table (std/int/max 32 (/ (length table) 2))) nil) nil)) nil) table))))) (let std/vector/hash/table/get* (lambda xs i some none (if (std/vector/hash/table/has? xs i) (do (some (std/vector/hash/table/get xs i)) nil) (do (none) nil)))) (let std/vector/hash/table/get (lambda table key (if (= (length table) 0) (vector) (do (let idx (std/int/hash table key)) (let bucket (get table idx)) (let index (std/vector/hash/table/find-index bucket key)) (if (>= index 0) (vector (get bucket index)) (vector)))))) (let std/vector/hash/table/update! (lambda table key init f (do (if (std/vector/hash/table/has? table key) (std/vector/hash/table/set! table key (f (snd (get (std/vector/hash/table/get table key) 0)))) (std/vector/hash/table/set! table key init)) nil))) (let std/vector/hash/table/update-or! (lambda table key missing present (do (if (std/vector/hash/table/has? table key) (std/vector/hash/table/set! table key (present (snd (get (std/vector/hash/table/get table key) 0)))) (std/vector/hash/table/set! table key (missing))) nil))) (let std/vector/hash/table/push-or! (lambda table key value (do (if (std/vector/hash/table/has? table key) (do (push! (snd (get (std/vector/hash/table/get table key) 0)) value) nil) (do (std/vector/hash/table/set! table key (vector value)) nil)) nil))) (let std/vector/hash/table/entries (lambda table (do (let out (vector)) (std/vector/hash/table/for-each table (lambda entry (set! out (length out) entry))) out))) (let std/vector/hash/table/keys (lambda table (do (let entries (std/vector/hash/table/entries table)) (let out (vector)) (mut i 0) (let len (length entries)) (while (< i len) (do (set! out (length out) (fst (get entries i))) (alter! i (+ i 1)))) out))) (let std/vector/hash/table/values (lambda table (do (let entries (std/vector/hash/table/entries table)) (let out (vector)) (mut i 0) (let len (length entries)) (while (< i len) (do (set! out (length out) (snd (get entries i))) (alter! i (+ i 1)))) out))) (let std/vector/hash/table/count (lambda arr (do (let table (std/vector/hash/table (std/int/max 64 (length arr)))) (mut i 0) (let len (length arr)) (while (< i len) (do (let key (get arr i)) (let hit (std/vector/hash/table/get table key)) (if (= (length hit) 0) (std/vector/hash/table/set! table key 1) (std/vector/hash/table/set! table key (+ (snd (get hit 0)) 1))) (alter! i (+ i 1)))) table))) (let std/vector/hash/table/drop! (lambda table keys (do (mut i 0) (let len (length keys)) (while (< i len) (do (std/vector/hash/table/remove! table (get keys i)) (alter! i (+ i 1))))))) (let std/vector/hash/table/keep (lambda table keys (do (let out (std/vector/hash/table (std/int/max 32 (length keys)))) (mut i 0) (let len (length keys)) (while (< i len) (do (let key (get keys i)) (let hit (std/vector/hash/table/get table key)) (if (> (length hit) 0) (do (std/vector/hash/table/set! out key (fst (get hit 0))) nil) nil) (alter! i (+ i 1)))) out))) (let std/vector/hash/table/merge! (lambda a b (do (let entries (std/vector/hash/table/entries b)) (mut i 0) (let len (length entries)) (while (< i len) (do (let entry (get entries i)) (std/vector/hash/table/set! a (fst entry) (snd entry)) (alter! i (+ i 1)))) a))) (let std/vector/hash/table/merge (lambda a b (do (let out (std/vector/hash/table/max-capacity a b)) (std/vector/hash/table/merge! out a) (std/vector/hash/table/merge! out b) out))) (let std/vector/hash/table/omit (lambda table keys (do (let out (std/vector/hash/table/merge (std/vector/hash/table 32) table)) (std/vector/hash/table/drop! out keys) out))) (let std/convert/vector->table (lambda entries (do (let out (std/vector/hash/table (std/int/max 32 (length entries)))) (mut i 0) (let len (length entries)) (while (< i len) (do (let entry (get entries i)) (std/vector/hash/table/set! out (fst entry) (snd entry)) (alter! i (+ i 1)))) out))) (let std/vector/hash/table/group-by (lambda xs fn (do (let out (std/vector/hash/table 32)) (mut i 0) (let len (length xs)) (while (< i len) (do (let item (get xs i)) (let key (fn item)) (let hit (std/vector/hash/table/get out key)) (if (= (length hit) 0) (do (std/vector/hash/table/set! out key (vector item)) nil) (do (push! (snd (get hit 0)) item) nil)) (alter! i (+ i 1)))) out))) (let std/int/min/3 (lambda a b c (std/int/min (std/int/min a b) c))) (let std/int/min/4 (lambda a b c d (std/int/min (std/int/min a b) (std/int/min c d)))) (let std/int/min/2 std/int/min) (let std/vector/char/damerau-levenshtein (lambda a b (do (let n (length a)) (let m (length b)) (let matrix (Matrix/new (lambda _ _ 0) (+ n 1) (+ m 1))) (mut i0 0) (while (<= i0 n) (do (let row (get matrix i0)) (set! row 0 i0) (alter! i0 (+ i0 1)))) (let first-row (get matrix 0)) (mut j0 0) (while (<= j0 m) (do (set! first-row j0 j0) (alter! j0 (+ j0 1)))) (mut i 1) (while (<= i n) (do (let current-row (get matrix i)) (let prev-row (get matrix (- i 1))) (mut j 1) (while (<= j m) (do (let a-char (get a (- i 1))) (let b-char (get b (- j 1))) (let replace-cost (if (=# a-char b-char) 0 1)) (let delete-cost (+ (get prev-row j) 1)) (let insert-cost (+ (get current-row (- j 1)) 1)) (let subst-cost (+ (get prev-row (- j 1)) replace-cost)) (let best (std/int/min/3 delete-cost insert-cost subst-cost)) (let with-transpose (if (and (> i 1) (> j 1) (=# a-char (get b (- j 2))) (=# (get a (- i 2)) b-char)) (std/int/min best (+ (get (get matrix (- i 2)) (- j 2)) 1)) best)) (set! current-row j with-transpose) (alter! j (+ j 1)))) (alter! i (+ i 1)))) (get (get matrix n) m)))) (let std/vector/char/autocorrect (lambda word dictionary (do (let f (get dictionary 0)) (variable best-word f) (mut best-dist (std/vector/char/damerau-levenshtein word f)) (mut i 1) (while (< i (length dictionary)) (do (let candidate (get dictionary i)) (let dist (std/vector/char/damerau-levenshtein word candidate)) (if (< dist best-dist) (do (set best-word candidate) (alter! best-dist dist))) (alter! i (+ i 1)))) (tuple (get best-word) best-dist)))) (let std/vector/char/join2 (lambda a b (do (let out (vector)) (mut i 0) (while (< i (length a)) (do (push! out (get a i)) (alter! i (+ i 1)))) (mut j 0) (while (< j (length b)) (do (push! out (get b j)) (alter! j (+ j 1)))) out))) (let std/vector/char/join3 (lambda a b c (std/vector/char/join2 (std/vector/char/join2 a b) c))) (let std/vector/char/diff/simple (lambda old new (do (let old-len (length old)) (let new-len (length new)) (mut prefix 0) (while (and (< prefix old-len) (< prefix new-len) (=# (get old prefix) (get new prefix))) (alter! prefix (+ prefix 1))) (mut old-suffix old-len) (mut new-suffix new-len) (while (and (> old-suffix prefix) (> new-suffix prefix) (=# (get old (- old-suffix 1)) (get new (- new-suffix 1)))) (do (alter! old-suffix (- old-suffix 1)) (alter! new-suffix (- new-suffix 1)))) (let delete-count (- old-suffix prefix)) (let insert-text (std/vector/slice new prefix new-suffix)) (tuple prefix (tuple delete-count insert-text))))) (let std/vector/char/apply-patch (lambda source patch (do (let pos (fst patch)) (let rest (snd patch)) (let delete-count (fst rest)) (let insert-text (snd rest)) (let before (std/vector/slice source 0 pos)) (let after (std/vector/slice source (+ pos delete-count) (length source))) (std/vector/char/join3 before insert-text after)))) (let std/vector/char/apply-patches (lambda source patches (do (mut result source) (mut i 0) (while (< i (length patches)) (do (alter! result (std/vector/char/apply-patch result (get patches i))) (alter! i (+ i 1)))) result))) (let std/text/history/new (lambda _ (tuple (string) (vector)))) (let std/text/history/source (lambda history (fst history))) (let std/text/history/patches (lambda history (snd history))) (let std/text/history/add! (lambda history new-source (do (let old-source (fst history)) (let patches (snd history)) (let patch (std/vector/char/diff/simple old-source new-source)) (push! patches patch) (tuple new-source patches)))) (let std/text/history/reconstruct (lambda history (do (let patches (snd history)) (std/vector/char/apply-patches (string) patches)))) (let std/char/space? (lambda c (or (=# c std/char/space) (=# c std/char/new-line) (=# c std/char/carriage-return) (=# c std/char/tab)))) (let std/vector/char/trim/left (lambda xs (do (let len (length xs)) (mut start 0) (while (and (< start len) (std/char/space? (get xs start))) (do (++ start))) (std/vector/slice xs start len)))) (let std/vector/char/trim/right (lambda xs (do (let len (length xs)) (mut end (- len 1)) (while (and (>= end 0) (std/char/space? (get xs end))) (do (-- end))) (std/vector/slice xs 0 (+ end 1))))) (let std/vector/char/trim (lambda xs (std/vector/char/trim/right (std/vector/char/trim/left xs)))) (let const std/fn/const) (let floor std/dec/floor) (let ceil std/dec/ceil) (let extreme std/vector/int/extreme) (let map/tuple (lambda fn xs (std/tuple/map xs fn))) (let map/fst (lambda fn xs (std/tuple/map/fst xs fn))) (let map/snd (lambda fn xs (std/tuple/map/snd xs fn))) (let flat-map (lambda fn xs (std/vector/flat-map xs fn))) (let map (lambda fn xs (std/vector/map xs fn))) (let filter (lambda fn? xs (std/vector/filter xs fn?))) (let reduce (lambda fn init xs (std/vector/reduce xs fn init))) (let for (lambda fn xs (std/vector/for xs fn))) (let for/i (lambda fn xs (std/vector/for/i xs fn))) (let each (lambda xs fn (do (std/vector/for xs fn) xs))) (let each/i (lambda xs fn (do (std/vector/for/i xs fn) xs))) (let transpose std/vector/3d/rotate) (let interleave std/vector/2d/interleave) (let intersperse (lambda xs x (std/vector/intersperse x xs))) (let every? (lambda fn? xs (std/vector/every? xs fn?))) (let some? (lambda fn? xs (std/vector/some? xs fn?))) (let empty? std/vector/empty?) (let not-empty? std/vector/not-empty?) (let exclude (lambda fn? xs (std/vector/filter xs (lambda x (not (fn? x)))))) (let select (lambda fn? xs (std/vector/filter xs fn?))) (let find (lambda fn? xs (std/vector/find-index xs fn?))) (let partition (lambda n xs (std/vector/partition xs n))) (let reverse std/vector/reverse) (let slice (lambda start end xs (std/vector/slice xs start end))) (let range std/vector/int/range) (let range/int std/vector/int/range) (let range/dec std/vector/dec/range) (let square std/int/square) (let expt (lambda b x (std/int/expt x b))) (let sqrt std/int/sqrt) (let expt/int (lambda b x (std/int/expt x b))) (let sqrt/int std/int/sqrt) (let expt/dec (lambda b x (std/dec/expt x b))) (let sqrt/dec std/dec/sqrt) (let log std/dec/log) (let odd? std/int/odd?) (let even? std/int/even?) (let odd/int? std/int/odd?) (let even/int? std/int/even?) (let odd/dec? std/dec/odd?) (let even/dec? std/dec/even?) (let one? std/int/one?) (let zero? std/int/zero?) (let one/int? std/int/one?) (let zero/int? std/int/zero?) (let one/dec? std/dec/one?) (let zero/dec? std/dec/zero?) (let space? std/char/space?) (let map/until (lambda fn fn? xs (std/vector/map/until xs fn fn?))) (let map/until/i (lambda fn fn? xs (std/vector/map/until/i xs fn fn?))) (let reduce/until (lambda fn fn? init xs (std/vector/reduce/until xs fn fn? init))) (let reduce/until/i (lambda fn fn? init xs (std/vector/reduce/until/i xs fn fn? init))) (let for/until (lambda fn fn? xs (std/vector/for/until xs fn fn?))) (let for/until/i (lambda fn fn? xs (std/vector/for/until/i xs fn fn?))) (let each/until (lambda fn fn? xs (do (std/vector/for/until xs fn fn?) xs))) (let each/until/i (lambda fn fn? xs (do (std/vector/for/until/i xs fn fn?) xs))) (let map/i (lambda fn xs (std/vector/map/i xs fn))) (let reduce/i (lambda fn init xs (std/vector/reduce/i xs fn init))) (let filter/i (lambda fn? xs (std/vector/filter/i xs fn?))) (let some/i? (lambda fn? xs (std/vector/some/i? xs fn?))) (let every/i? (lambda fn? xs (std/vector/every/i? xs fn?))) (let ones std/vector/int/ones) (let zeroes std/vector/int/zeroes) (let ones/int std/vector/int/ones) (let zeroes/int std/vector/int/zeroes) (let ones/dec std/vector/dec/ones) (let zeroes/dec std/vector/dec/zeroes) (let truths std/vector/bool/true) (let falses std/vector/bool/false) (let positive? std/int/positive?) (let negative? std/int/negative?) (let invert std/int/invert) (let negative-one? std/int/negative-one?) (let divisible? (lambda y x (std/int/divisible? x y))) (let positive/int? std/int/positive?) (let negative/int? std/int/negative?) (let invert/int std/int/invert) (let negative-one/int? std/int/negative-one?) (let divisible/int? (lambda y x (std/int/divisible? x y))) (let positive/dec? std/dec/positive?) (let negative/dec? std/dec/negative?) (let invert/dec std/dec/invert) (let negative-one/dec? std/dec/negative-one?) (let divisible/dec? (lambda y x (std/dec/divisible? x y))) (let upper std/char/upper) (let lower std/char/lower) (let match? std/vector/char/equal?) (let digit? std/char/digit?) (let fill std/vector/2d/fill) (let max std/int/max) (let min std/int/min) (let max/int std/int/max) (let min/int std/int/min) (let max/dec std/dec/max) (let min/dec std/dec/min) (let maximum std/vector/int/maximum) (let minimum std/vector/int/minimum) (let maximum/int std/vector/int/maximum) (let minimum/int std/vector/int/minimum) (let maximum/dec std/vector/dec/maximum) (let minimum/dec std/vector/dec/minimum) (let gt/int? (lambda a b (> b a))) (let lt/int? (lambda a b (< b a))) (let gte/int? (lambda a b (>= b a))) (let lte/int? (lambda a b (<= b a))) (let gt/dec? (lambda a b (>. b a))) (let lt/dec? (lambda a b (<. b a))) (let gte/dec? (lambda a b (>=. b a))) (let lte/dec? (lambda a b (<=. b a))) (let gt/bool? (lambda a b (and (=? a true) (=? b false)))) (let lt/bool? (lambda a b (and (=? a false) (=? b true)))) (let and? (lambda a b (and (=? a true) (=? b true)))) (let or? (lambda a b (or (=? a true) (=? b true)))) (let not? (lambda x (not x))) (let abs std/int/abs) (let abs/int std/int/abs) (let abs/dec std/dec/abs) (let first std/vector/first) (let last std/vector/last) (let pair (lambda a b (tuple a b))) (let product std/vector/int/product) (let product/int std/vector/int/product) (let product/dec std/vector/dec/product) (let sum std/vector/int/sum) (let sum/int std/vector/int/sum) (let sum/dec std/vector/dec/sum) (let avg std/int/average) (let avg/int std/int/average) (let avg/dec std/dec/average) (let mean std/vector/int/mean) (let median std/vector/int/median) (let mean/int std/vector/int/mean) (let mean/dec std/vector/dec/mean) (let median/int std/vector/int/median) (let median/dec std/vector/dec/median) (let zip std/vector/tuple/zip) (let unzip std/vector/tuple/unzip) (let zip-with (lambda f xs ys (std/vector/tuple/zip-with xs ys f))) (let window (lambda n xs (std/vector/sliding-window xs n))) (let flat std/vector/flat-one) (let enumerate std/vector/enumerate) (let clamp (lambda limit x (std/int/clamp x limit))) (let clamp-range (lambda start end x (std/int/clamp-range x start end))) (let clamp/int (lambda limit x (std/int/clamp x limit))) (let clamp-range/int (lambda start end x (std/int/clamp-range x start end))) (let clamp/dec (lambda limit x (std/dec/clamp x limit))) (let clamp-range/dec (lambda start end x (std/dec/clamp-range x start end))) (let at std/vector/at) (let scan (lambda fn xs (std/vector/adjacent-difference xs fn))) (let cycle std/vector/cycle) (let replicate std/vector/replicate) (let cartesian-product std/vector/cartesian-product) (let lcm std/int/lcm) (let gcd std/int/gcd) (let delta std/int/delta) (let delta/int std/int/delta) (let delta/dec std/dec/delta) (let map/adjacent (lambda fn xs (std/vector/map/adjacent xs fn))) (let buckets std/vector/buckets) (let count/char (lambda x xs (std/vector/char/count xs x))) (let count/int (lambda x xs (std/vector/int/count xs x))) (let count/dec (lambda x xs (std/vector/dec/count xs x))) (let count/bool (lambda x xs (std/vector/bool/count xs x))) (let count (lambda fn? xs (std/vector/count-of xs fn?))) (let points (lambda fn? xs (std/vector/3d/points xs fn?))) (let unique/int std/vector/int/unique) (let unique/char std/vector/char/unique) (let permutation std/vector/permutations) (let combination/pairs std/vector/tuple/unique-pairs) (let combination std/vector/combinations) (let combination/n (lambda n xs (std/vector/combinations/n xs n))) (let subset std/vector/subset) (let in-bounds? std/vector/in-bounds?) (let take/first (lambda n xs (std/vector/take xs n))) (let drop/first (lambda n xs (std/vector/drop xs n))) (let take/last (lambda n xs (std/vector/take/last xs n))) (let drop/last (lambda n xs (std/vector/drop/last xs n))) (let take/while (lambda fn? xs (std/vector/take/while xs fn?))) (let drop/while (lambda fn? xs (std/vector/drop/while xs fn?))) (let true/option std/true/option) (let false/option std/false/option) (let resolve/option (lambda fn df xs (std/vector/option/resolve xs fn df))) (let call (lambda fn xs (std/fn/exec xs fn))) (let copy std/vector/copy) (let sort (lambda fn xs (do (let out (std/vector/copy xs)) (std/vector/sort! out fn) out))) (let sort/bool/desc (lambda xs (do (let out (vector)) (let trues (count (lambda x x) xs)) (mut i 0) (while (< i trues) (push! out true) (++ i)) (while (< i (length xs)) (push! out false) (++ i)) out))) (let sort/bool/asc (lambda xs (do (let out (vector)) (let falses (count (lambda x (not x)) xs)) (mut i 0) (while (< i falses) (push! out false) (++ i)) (while (< i (length xs)) (push! out true) (++ i)) out))) (let neighborhood (lambda directions y x fn xs (std/vector/3d/adjacent xs directions y x fn))) (let neighborhood/moore std/vector/3d/moore-neighborhood) (let neighborhood/diagonal std/vector/3d/diagonal-neighborhood) (let neighborhood/kernel std/vector/3d/kernel-neighborhood) (let neighborhood/von-neumann std/vector/3d/von-neumann-neighborhood) (let group (lambda fn xs (std/vector/hash/table/group-by xs fn))) (let tail (lambda xs (std/vector/slice xs 1 (length xs)))) (let head (lambda xs (std/vector/slice xs 0 (- (length xs) 1)))) (let fp/mul (lambda b a (* a b))) (let fp/div (lambda b a (/ a b))) (let fp/add (lambda b a (+ a b))) (let fp/sub (lambda b a (- a b))) (let fp/emod (lambda b a (emod a b))) (let fp/mod (lambda b a (mod a b))) (let cond/dispatch (lambda fn? a b x (if (fn? x) a b))) (let split (lambda ys str (do (if (empty? ys) (vector str) (do (let out (vector)) (mut i 0) (mut start 0) (let len-str (length str)) (let len-ys (length ys)) (while (< i len-str) (do (mut matched? false) (if (and (<= (+ i len-ys) len-str) (=# (get str i) (get ys 0))) (do (alter! matched? true) (mut j 1) (while (and matched? (< j len-ys)) (do (if (not (=# (get str (+ i j)) (get ys j))) (alter! matched? false)) (alter! j (+ j 1)))))) (if matched? (do (push! out (slice start i str)) (alter! start (+ i len-ys)) (alter! i (+ i len-ys))) (alter! i (+ i 1))))) (push! out (slice start len-str str)) out))))) (let join (lambda str xs (do (let out (vector)) (mut i 0) (let len-xs (length xs)) (while (< i len-xs) (do (let current (get xs i)) (mut j 0) (while (< j (length current)) (do (push! out (get current j)) (alter! j (+ j 1)))) (if (< (+ i 1) len-xs) (do (mut k 0) (while (< k (length str)) (do (push! out (get str k)) (alter! k (+ k 1)))))) (alter! i (+ i 1)))) out))) (let join/lines (lambda xs (join (vector nl) xs))) (let join/commas (lambda xs (join (string 44) xs))) (let split/lines std/vector/char/lines) (let split/words std/vector/char/words) (let split/commas std/vector/char/commas) (let replace (lambda a b xs (|> xs (split a) (join b)))) (let graph/project-path (lambda col path (map (lambda i (get col i)) path))) (let graph/path->nodes (lambda from-col to-col path (if (empty? path) (vector) (do (let out (vector (get from-col (get path 0)))) (for (lambda i (push! out (get to-col i))) path) out)))) (let graph/rotate (lambda start xs (do (let out (vector)) (mut i 0) (let len (length xs)) (while (< i len) (do (push! out (get xs (mod (+ start i) len))) (alter! i (+ i 1)))) out))) (let graph/cycle/min-rotation (lambda nodes (if (<= (length nodes) 1) 0 (do (let cycle-len (- (length nodes) 1)) (mut best 0) (mut i 1) (while (< i cycle-len) (do (if (std/vector/char/lesser? (get nodes i) (get nodes best)) (alter! best i) nil) (alter! i (+ i 1)))) best)))) (let graph/normalize-cycle (lambda nodes (if (<= (length nodes) 1) nodes (do (let start (graph/cycle/min-rotation nodes)) (let core (graph/rotate start (slice 0 (- (length nodes) 1) nodes))) (cons core (vector (get core 0))))))) (let graph/normalize-path (lambda from-col to-col path (if (empty? path) (vector) (graph/rotate (graph/cycle/min-rotation (graph/path->nodes from-col to-col path)) path)))) (let graph/cycle-key (lambda from-col to-col path (do (let normalized-nodes (graph/normalize-cycle (graph/path->nodes from-col to-col path))) (let normalized-path (graph/normalize-path from-col to-col path)) (cons (join (string 45 62) normalized-nodes) (string 58 58) (join (string 44) (map Integer->String normalized-path)))))) (let graph/outgoing-by (lambda from-col rows (reduce (lambda (a i) (do (let from (get from-col i)) (if (Table/has? from a) (push! (Table/get-unsafe from a) i) (Table/set! a from (vector i))) a)) (Table/new) rows))) (let graph/simple-cycle? (lambda from-col to-col path (do (let nodes (graph/path->nodes from-col to-col path)) (if (or (< (length nodes) 3) (not (match? (get nodes 0) (last nodes)))) false (do (let seen (Set/new)) (mut i 0) (mut ok true) (let stop (- (length nodes) 1)) (while (and ok (< i stop)) (do (let node (get nodes i)) (if (Set/has? node seen) (alter! ok false) (Set/add! seen node)) (alter! i (+ i 1)))) ok))))) (let graph/find-cycles (lambda rows from-col to-col next-ok? cycle-ok? (do (let outgoing (graph/outgoing-by from-col rows)) (let seen (Set/new)) (let out (vector)) (let contains-node? (lambda node nodes (some? (lambda x (match? x node)) nodes))) (letrec dfs (lambda origin current visited path (if (Table/has? current outgoing) (for (lambda next-i (do (let next-to (get to-col next-i)) (if (next-ok? path next-i) (if (match? next-to origin) (do (let full-path (cons path (vector next-i))) (if (and (graph/simple-cycle? from-col to-col full-path) (cycle-ok? full-path)) (do (let key (graph/cycle-key from-col to-col full-path)) (if (not (Set/has? key seen)) (do (Set/add! seen key) (push! out full-path)) nil)) nil)) (if (not (contains-node? next-to visited)) (dfs origin next-to (cons visited (vector next-to)) (cons path (vector next-i))) nil)) nil))) (Table/get-unsafe current outgoing)) nil))) (for (lambda start-i (dfs (get from-col start-i) (get to-col start-i) (vector (get from-col start-i) (get to-col start-i)) (vector start-i))) rows) out))) (let graph/find-cycles/increasing-time (lambda rows from-col to-col time-col min-length max-span-seconds (graph/find-cycles rows from-col to-col (lambda (path next-i) (> (get time-col next-i) (get time-col (at path -1)))) (lambda (path) (and (>= (length path) min-length) (<= (- (get time-col (at path -1)) (get time-col (get path 0))) max-span-seconds)))))) (let graph/has-cycle? (lambda rows from-col to-col (not (empty? (graph/find-cycles rows from-col to-col (lambda (path next-i) true) (lambda (path) true)))))) (let floyd/cycle? (lambda next eq? start limit (do (&mut turtle start) (&mut hare start) (mut steps 0) (mut found? false) (while (and (not found?) (< steps limit)) (&alter! turtle (next (&get turtle))) (&alter! hare (next (&get hare))) (&alter! hare (next (&get hare))) (alter! found? (eq? (&get hare) (&get turtle))) (++ steps)) found?))) (let autocorrect (lambda dict word (std/vector/char/autocorrect word dict))) (let trim std/vector/char/trim) (let trim/left std/vector/char/trim/left) (let trim/right std/vector/char/trim/right) (let Tuple/new (lambda a b (tuple a b))) (let Tuple/swap std/tuple/swap) (let Tuple/map std/tuple/map) (let Tuple/map/fst std/tuple/map/fst) (let Tuple/map/snd std/tuple/map/snd) (let Vector->Tuple (lambda fn1 fn2 xs (std/convert/vector->tuple xs fn1 fn2))) (let Tuple/int/add std/tuple/int/add) (let Tuple/int/sub std/tuple/int/sub) (let Tuple/int/mul std/tuple/int/mul) (let Tuple/int/div std/tuple/int/div) (let Tuple/bool/eq? (lambda (tuple a b) (=? a b))) (let Tuple/int/eq? (lambda (tuple a b) (= a b))) (let Tuple/char/eq? (lambda (tuple a b) (=# a b))) (let Tuple/dec/eq? (lambda (tuple a b) (=. a b))) (let Tuple/bool/not-eq? (lambda (tuple a b) (not (=? a b)))) (let Tuple/int/not-eq? (lambda (tuple a b) (not (= a b)))) (let Tuple/char/not-eq? (lambda (tuple a b) (not (=# a b)))) (let Tuple/dec/not-eq? (lambda (tuple a b) (not (=. a b)))) (let Int->Alphabet std/convert/int->char/alphabet) (let Integer->Chars std/convert/integer->string) (let Integer->String std/convert/integer->string) (let Dec->Chars std/convert/dec->string) (let Dec->String std/convert/dec->string) (let String->Vector (lambda ch xs (std/convert/string->vector xs ch))) (let Vector->String (lambda ch xs (std/convert/vector->string xs ch))) (let Chars->Digits std/convert/chars->digits) (let Digits->Integer std/convert/digits->integer) (let Chars->Integer std/convert/chars->integer) (let String->Integer std/convert/chars->integer) (let String->Dec std/convert/chars->dec) (let Chars->Dec std/convert/chars->dec) (let Integer->Bits std/convert/integer->bits) (let Char->Digit std/convert/char->digit) (let Digit->Char std/convert/digit->char) (let Integer->Digits std/convert/integer->digits) (let Digits->Chars std/convert/digits->chars) (let Set->Vector std/convert/set->vector) (let Vector->Set std/convert/vector->set) (let Vector/equal? (lambda fn? xs ys (std/vector/equal? xs ys fn?))) (let Que/new std/vector/deque/new) (let Que/empty? std/vector/deque/empty?) (let Que/not-empty? std/vector/queue/not-empty?) (let Que/empty! (lambda q (do (std/vector/deque/empty! q) nil))) (let Que/enque! (lambda xs v (do (std/vector/queue/enqueue! xs v) nil))) (let Que/deque! (lambda queue (do (std/vector/deque/tail! queue) nil))) (let Que/peek std/vector/deque/first) (let Que/push! (lambda xs v (do (std/vector/deque/append! xs v) nil))) (let Que/pop! (lambda queue (do (std/vector/deque/head! queue) nil))) (let Que/prepend! (lambda xs v (do (std/vector/deque/prepend! xs v) nil))) (let Que/first std/vector/deque/first) (let Que/last std/vector/deque/last) (let Que/tail! (lambda queue (do (std/vector/deque/tail! queue) nil))) (let Que/append! (lambda xs v (do (std/vector/deque/append! xs v) nil))) (let Vector->Que std/convert/vector->deque) (let Que->Vector std/convert/deque->vector) (let Que/get std/vector/deque/get) (let Que/length std/vector/deque/length) (let Que/at (lambda i xs (if (< i 0) (std/vector/deque/get xs (+ (length xs) i)) (std/vector/deque/get xs i)))) (let BigInt/add std/int/big/add) (let BigInt/sub std/int/big/sub) (let BigInt/mul std/int/big/mul) (let BigInt/lte? std/int/big/less-or-equal?) (let BigInt/gte? std/int/big/greater-or-equal?) (let BigInt/lt? std/int/big/less-than?) (let BigInt/gt? std/int/big/greater-than?) (let BigInt/equal? std/int/big/equal?) (let BigInt/div std/int/big/div) (let BigInt/mod std/int/big/mod) (let BigInt/square std/int/big/square) (let BigInt/div/floor std/int/big/floor/div) (let BigInt/div/ceal std/int/big/ceil/div) (let BigInt/sum std/vector/int/big/sum) (let BigInt/product std/vector/int/big/product) (let BigInt/new std/int/big/new) (let BigInt/pow std/int/big/pow) (let BigInt/expt std/int/big/expt) (let BigInt/range std/vector/int/big/range) (let SignedBigInt/zero std/int/big/signed/zero) (let SignedBigInt/one std/int/big/signed/one) (let SignedBigInt/negative? std/int/big/signed/negative?) (let SignedBigInt/digits std/int/big/signed/digits) (let SignedBigInt/abs std/int/big/signed/abs) (let SignedBigInt/zero? std/int/big/signed/zero?) (let SignedBigInt/normalize std/int/big/signed/normalize) (let SignedBigInt/new std/int/big/signed/new) (let SignedBigInt/negate std/int/big/signed/negate) (let SignedBigInt/equal? std/int/big/signed/equal?) (let SignedBigInt/lt? std/int/big/signed/lt?) (let SignedBigInt/lte? std/int/big/signed/lte?) (let SignedBigInt/gt? std/int/big/signed/gt?) (let SignedBigInt/gte? std/int/big/signed/gte?) (let SignedBigInt/add std/int/big/signed/add) (let SignedBigInt/sub std/int/big/signed/sub) (let SignedBigInt/mul std/int/big/signed/mul) (let SignedBigInt/square std/int/big/signed/square) (let SignedBigInt/pow std/int/big/signed/pow) (let SignedBigInt/expt std/int/big/signed/expt) (let SignedBigInt/div std/int/big/signed/div) (let SignedBigInt/mod std/int/big/signed/mod) (let SignedBigInt/div/floor std/int/big/signed/floor/div) (let SignedBigInt/div/ceil std/int/big/signed/ceil/div) (let SignedBigInt/to-string std/int/big/signed/to-string) (let SignedBigInt/range std/vector/int/big/signed/range) (let SignedBigInt/sum std/vector/int/big/signed/sum) (let SignedBigInt/product std/vector/int/big/signed/product) (let Vector/push! std/vector/push!) (let Vector/pop! std/vector/pop!) (let Vector/last std/vector/last) (let Vector/first std/vector/first) (let Vector/pull! std/vector/pop-and-get!) (let Vector/get-unsafe (lambda idx xs (get xs idx))) (let Vector/get* (lambda idx xs (if (and (>= idx 0) (< idx (length xs))) (tuple true (vector (get xs idx))) (tuple false (vector))))) (let Vector/at! (lambda idx xs (std/vector/at xs idx))) (let Vector/at* (lambda idx xs (if (< idx (length xs)) (tuple true (vector (std/vector/at xs idx))) (tuple false (vector))))) (let Vector/set! (lambda xs i v (set! xs i v))) (let Vector/length std/vector/length) (let Vector/append! (lambda xs x (std/vector/append! xs x))) (let Vector/cons! (lambda xs x (std/vector/cons! xs x))) (let Vector/cons (lambda x xs (std/vector/cons xs x))) (let Vector/new (lambda fn n (std/vector/2d/fill n fn))) (let Matrix/new (lambda fn w h (std/vector/3d/fill w h fn))) (let String/equal? std/vector/char/equal?) (let String/lte? std/vector/char/lesser-or-equal?) (let String/gte? std/vector/char/greater-or-equal?) (let String/lt? std/vector/char/lesser?) (let String/gt? std/vector/char/greater?) (let String/quote (lambda str (cons (string 39) str (string 39)))) (let String/dquote (lambda str (cons (vector dq) str (vector dq)))) (let Char/eq? (lambda a b (=# a b))) (let Int/eq? (lambda a b (= a b))) (let Bool/eq? (lambda a b (=? a b))) (let Dec/eq? (lambda a b (=. a b))) (let Table/size std/vector/flat/length) (let Table/new (lambda (std/vector/hash/table 32))) (let Table/new/capacity (lambda n (std/vector/hash/table n))) (let Table/entries std/vector/hash/table/entries) (let Table/keys std/vector/hash/table/keys) (let Table/values std/vector/hash/table/values) (let Table/get (lambda key table (std/vector/hash/table/get table key))) (let Table/get* (lambda key table (if (std/vector/hash/table/has? table key) (tuple true (vector (snd (get (std/vector/hash/table/get table key) 0)))) (tuple false (vector))))) (let Table/get-unsafe (lambda key table (snd (get (std/vector/hash/table/get table key) 0)))) (let Table/has? (lambda key table (std/vector/hash/table/has? table key))) (let Table/set! (lambda table key value (do (std/vector/hash/table/set! table key value) nil))) (let Table/update! std/vector/hash/table/update!) (let Table/update-or! std/vector/hash/table/update-or!) (let Table/push-or! std/vector/hash/table/push-or!) (let Table/remove! (lambda table key (do (std/vector/hash/table/remove! table key) nil))) (let Table/count std/vector/hash/table/count) (let Table/drop! (lambda table keys (std/vector/hash/table/drop! table keys))) (let Table/keep (lambda keys table (std/vector/hash/table/keep table keys))) (let Table/omit (lambda keys table (std/vector/hash/table/omit table keys))) (let Table/merge! (lambda table other (do (std/vector/hash/table/merge! table other) nil))) (let Table/merge (lambda other table (std/vector/hash/table/merge table other))) (let Table/resize! (lambda table n (do (std/vector/hash/table/resize! table n) nil))) (let Table/compact! (lambda table (do (std/vector/hash/table/compact! table) nil))) (let Vector->Table std/convert/vector->table) (let Set/size std/vector/flat/length) (let Set/values std/vector/flat-one) (let Set/new (lambda (std/vector/hash/set 32))) (let Set/new/capacity (lambda n (std/vector/hash/set n))) (let Set/intersection std/vector/hash/set/intersection) (let Set/difference std/vector/hash/set/difference) (let Set/xor std/vector/hash/set/xor) (let Set/union std/vector/hash/set/union) (let Set/add! (lambda table item (do (std/vector/hash/set/add! table item) nil))) (let Set/remove! (lambda table item (do (std/vector/hash/set/remove! table item) nil))) (let Set/has? (lambda item table (std/vector/hash/set/has? table item))) (let Set/resize! (lambda table n (do (std/vector/hash/set/resize! table n) nil))) (let Set/compact! (lambda table (do (std/vector/hash/set/compact! table) nil))) (let Vector->Set std/convert/vector->set/dynamic) (let Heap/compare? (lambda i j fn? heap (std/heap/greater? heap i j fn?))) (let Heap/peek! std/heap/peek) (let Heap/push! (lambda heap value fn (std/heap/push! heap value fn))) (let Heap/pop! (lambda heap fn (std/heap/pop! heap fn))) (let Heap/replace! (lambda heap value fn (std/heap/replace! heap value fn))) (let Heap/empty? std/vector/empty?) (let Heap/not-empty? std/vector/not-empty?) (let Heap/empty! std/vector/empty!) (let Vector->Heap (lambda fn xs (std/convert/vector->heap xs fn))) (let Matrix->String (comp (map (Vector->String sp)) (Vector->String nl))) (let Date/iso->Vector (lambda ts (vector (String->Integer (slice 0 4 ts)) (String->Integer (slice 5 7 ts)) (String->Integer (slice 8 10 ts))))) (let Date/iso-z->Vector (lambda ts (vector (String->Integer (slice 0 4 ts)) (String->Integer (slice 5 7 ts)) (String->Integer (slice 8 10 ts)) (String->Integer (slice 11 13 ts)) (String->Integer (slice 14 16 ts)) (String->Integer (slice 17 19 ts))))) (let Date/iso->Vector (lambda ts (if (= (length ts) 10) (vector (String->Integer (slice 0 4 ts)) (String->Integer (slice 5 7 ts)) (String->Integer (slice 8 10 ts)) 0 0 0) (vector (String->Integer (slice 0 4 ts)) (String->Integer (slice 5 7 ts)) (String->Integer (slice 8 10 ts)) (String->Integer (slice 11 13 ts)) (String->Integer (slice 14 16 ts)) (String->Integer (slice 17 19 ts)))))) (let Date/year (lambda dt (get dt 0))) (let Date/month (lambda dt (get dt 1))) (let Date/day (lambda dt (get dt 2))) (let Date/hour (lambda dt (get dt 3))) (let Date/ymd (lambda (vector a b c) (vector a b c))) (let Date/ymdh (lambda (vector a b c d) (vector a b c d))) (let Timestamp/iso-z->seconds (lambda (ts) (+ (* (- (String->Integer (slice 8 10 ts)) 1) 86400) (* (String->Integer (slice 11 13 ts)) 3600) (* (String->Integer (slice 14 16 ts)) 60) (String->Integer (slice 17 19 ts))))) (let csv/char/cr (get (string 13) 0)) (let csv/header fst) (let csv/rows snd) (let csv/empty-cell? (lambda cell (= (length cell) 0))) (let csv/trim-cr (lambda cell (if (and (not-empty? cell) (=# (last cell) csv/char/cr)) (head cell) cell))) (let csv/split-line (lambda cell-delim line (|> line (csv/trim-cr) (split cell-delim) (map csv/trim-cr)))) (let csv/read/simple (lambda text cell-delim (do (let raw-lines (split (vector nl) text)) (let lines (filter not-empty? (map csv/trim-cr raw-lines))) (let header (if (empty? lines) (vector) (csv/split-line cell-delim (get lines 0)))) (let rows (if (< (length lines) 2) (vector) (map (lambda line (csv/split-line cell-delim line)) (tail lines)))) (tuple header rows)))) (let csv/write/row (lambda row cell-delim (join cell-delim row))) (let csv/write/simple (lambda header rows cell-delim (do (let lines (vector)) (if (not-empty? header) (push! lines (csv/write/row header cell-delim))) (each rows (lambda row (push! lines (csv/write/row row cell-delim)))) (join (vector nl) lines)))) (let csv/write/parsed (lambda parsed cell-delim (csv/write/simple (csv/header parsed) (csv/rows parsed) cell-delim))) (let csv/header-index (lambda header header-name (find (lambda cell (match? cell header-name)) header))) (let csv/cell-at (lambda row idx (if (and (>= idx 0) (< idx (length row))) (get row idx) (string)))) (let csv/as-string (lambda cell default (if (csv/empty-cell? cell) default cell))) (let csv/as-int (lambda cell default (if (csv/empty-cell? cell) default (String->Integer cell)))) (let csv/as-decimal (lambda cell default (if (csv/empty-cell? cell) default (String->Dec cell)))) (let csv/as-bool (lambda cell default (if (csv/empty-cell? cell) default (do (let lowered (map lower cell)) (cond (or (match? lowered (string 116 114 117 101)) (match? lowered (string 49)) (match? lowered (string 121 101 115))) true (or (match? lowered (string 102 97 108 115 101)) (match? lowered (string 48)) (match? lowered (string 110 111))) false default))))) (let csv/column/by (lambda parsed header-name reader default (do (let header (csv/header parsed)) (let rows (csv/rows parsed)) (let idx (csv/header-index header header-name)) (if (< idx 0) (vector) (map (lambda row (reader (csv/cell-at row idx) default)) rows))))) (let csv/column/string (lambda parsed header-name default (csv/column/by parsed header-name csv/as-string default))) (let csv/column/int (lambda parsed header-name default (csv/column/by parsed header-name csv/as-int default))) (let csv/column/decimal (lambda parsed header-name default (csv/column/by parsed header-name csv/as-decimal default))) (let csv/column/bool (lambda parsed header-name default (csv/column/by parsed header-name csv/as-bool default))) (letmacro with-csv-columns/from ((parsed body) (qq (uq body))) ((parsed name header reader default . rest) (qq (block (let (uq name) ((uq reader) (uq parsed) (uq header) (uq default))) (with-csv-columns/from (uq parsed) (uqs rest)))))) (letmacro with-csv-columns (lambda text cell-delim . rest (do (let parsed (gensym)) (qq ((lambda (uq parsed) (with-csv-columns/from (uq parsed) (uqs rest))) (csv/read/simple (uq text) (uq cell-delim))))))))
